# VControlHub 深度检查记录（2026-09-07）

> 本报告前半部分保留首轮检查证据。2026-09-08 延续审查已进行了共享 UI 整理、大文件库分页与同步优化，当前进度及新验证结果见文末和 `review-worklog-2026-09-07.md`。首轮“通过”只代表当时快照，不替代后续修改的门禁。

最新门禁：`continue20` 生产模式构建和运行时、独立全项目 TypeScript `continue22`、完整 ESLint 加最新定向检查通过。最终完整覆盖率 **650 文件、5,491 项通过，15 项可选集成跳过**，行 77.37%、语句 74.58%、分支 64.64%、函数 73.84%，达到仓库门槛。真实数据库另有 **4 文件 15 项通过**。业务回归 47/50，3 项响应式等待夹具修正后，最终三浏览器 **42/42 全部通过**，包括原失败的页面遍历；不将分次运行写成同次 50/50。全站 368 组视觉/axe、18 组打开弹窗检查完成，取样边界详见工作记录。修改尚未发布。

## 范围与环境

- 检查基线：`37015a49`。修改保留在 `/opt/VControlHub` 工作区，尚未提交、推送或部署。
- 生产目录正在提供服务，因此构建、数据库迁移和浏览器写操作均在 `/tmp/vcontrolhub-audit-20260907` 隔离工作树及独立 PostgreSQL 数据库中执行。
- 生产 Next、worker、SSH WebSocket 服务未重启。预览地址：`http://127.0.0.1:3107`，仅监听本机。
- 检查包括页面与 API 目录、权限调用、国际化、静态可访问性、前端异步状态、文件传输、会话认证、数据库连接池、安装脚本、单元测试、数据库集成和浏览器操作。

## 已修复问题

| 问题 | 影响与处理 | 主要位置 |
| --- | --- | --- |
| 失效会话在 Server Component 中删除 Cookie | 触发 Next.js 渲染异常，不能正常回到登录页。改用请求级缓存的会话读取，并在认证失败时重定向；同时消除页面守卫与布局重复校验 | `src/lib/auth/require-session.ts` |
| 两步验证失败被误报为网络故障 | 浏览器实测过期验证码得到 400，但 UI 捕获所有异常后只显示网络错误。现在展示 API 验证反馈，真正的传输异常仍使用本地化网络提示，并验证重新输入可成功 | `src/app/login/verify-2fa/verify-2fa-form.tsx` |
| WebDAV 绝对 Destination 被误拒绝 | 内部请求 origin 与公开访问地址不同，合法 MOVE 被判为跨来源。校验额外接受明确配置的站点 origin，继续拒绝任意第三方 origin 和跨节点路径，不信任转发请求头 | `src/lib/webdav/handler.ts` |
| WebDAV 目录查询静默漏文件 | 真实 PostgreSQL 将未明确类型的 `substring` 绑定参数解析到正则重载，导致直接子项过滤结果为空；改在 SQL 中计算整数起始位置，同时避免 JavaScript UTF-16 长度与 PostgreSQL 字符数不一致 | `src/lib/webdav/handler-internals.ts` |
| 数据库连接池参数未传给实际驱动 | Prisma 风格 URL 参数不会设置 `pg.Pool` 的容量和空闲超时。显式传入驱动选项，保留旧 URL 覆盖规则并校验范围 | `src/lib/db.ts`、`.env.example` |
| 切换筛选条件后旧轮询响应覆盖新状态 | 为 fetcher 引入版本标记，忽略过期成功、错误和 loading 变更，保留请求合并机制 | `src/lib/http/use-resource-polling.ts` |
| 离开文件页后旧请求改写浏览器地址 | 卸载时取消并使请求引用失效，即使传输层仍然返回结果也不再改写历史记录 | `src/app/files/use-file-browser-listing.ts` |
| 分享选择器导航与刷新请求竞态 | 初始加载、目录切换和手动刷新使用统一取消机制；切换时清空旧列表 | `src/app/shares/share-file-picker.tsx` |
| 批量分享部分成功后重试重复创建 | 每完成一项立即保留链接并移出待处理选择；失败后只重试未完成项，执行期间锁定相关控件并防止重入 | 同上 |
| 剪贴板失败仍提示已复制 | 成功提示依据实际复制结果；失败时保留生成链接并显示准确反馈，清理复制状态计时器 | 分享选择器、`src/app/files/share-file-button.tsx` |
| 并发分片上传进度回退、失败后重试与旧上传重叠 | 合并服务端确认的分片集合；失败后停止派发新分片，并等待在途请求全部结束后才向调用方报错 | `src/components/storage/storage-chunked-upload.ts` |
| 窄屏分享列表文件名列被挤压 | 手机隐藏类型列，缩小固定列并约束节点选择器宽度；清空按钮保持单行，新增 320px 文件名宽度回归 | 分享选择器 |
| 平板审计表格列拥挤、表头与内容错位 | 完整表格移至 xl 断点，较小视口使用紧凑记录；表头与行采用相同间距 | `src/app/audit/audit-client.tsx` |
| 安装复制无关缓存与测试产物 | rsync/tar 同时排除根目录 npm/cache、覆盖率、浏览器产物及本机运行环境文件；修正测试替身以跟随真实排除参数 | `deploy/install.sh`、`deploy/__tests__/preflight.test.ts` |

上述为首轮修复，当时未修改数据库结构。后续 UI 整理范围已扩大至共享样式、组件及多个业务页面，详见延续记录。

## 性能结果

- 连接池容量与空闲超时现已作用于真正的 PostgreSQL 驱动；新增测试直接检查适配器内部池配置，不建立数据库连接。限制按进程生效，部署多进程时仍需计算总连接预算。
- 页面守卫复用 React 请求级会话缓存，避免重复数据库查询；轮询继续合并并发刷新，取消过期列表请求减少无效工作。
- 分片确认集合单调累积，失败后的重试不会与旧批次重叠。
- 安装脚本排除了本机约 550 MB 的缓存目录；原先超过 15 秒的对应测试在修复后单独复测约 4.53 秒完成。此值是测试环境结果，不能视为所有部署的固定提速比例。
- 最终 Next.js 生产构建与运行时打包通过。该环境构建需 `NODE_OPTIONS=--max-old-space-size=4096`；已有部署流程包含内存设置。

## 验证记录

| 检查 | 结果 |
| --- | --- |
| TypeScript 与 ESLint | 通过 |
| 最终隔离生产构建、server/worker/SSH 运行时打包 | 通过；最后含 WebDAV SQL 修复的构建退出状态为 0，部署资产校验也通过 |
| 路由目录 | 54 个页面、181 个 API 路由、54 个权限标识 |
| RBAC 静态审计 | 632 个调用点，无目录漂移 |
| 静态可访问性 | 424 个文件、368 个字段，无扫描器标记的字段或按钮问题 |
| 国际化 | 引用 5,178 个键，中英文各 5,205 个键，无缺失或语言不匹配；27 个候选未引用键需结合动态前缀判断 |
| API 文案审计 | 411 条既有基线记录，本次未新增回归 |
| 依赖安全审计 | `npm audit` 报告 0 个已知漏洞 |
| PostgreSQL 迁移与初始化 | 隔离数据库 66 次迁移和 seed 成功 |
| PostgreSQL advisory lock 集成 | 2 项通过 |
| worker 初始化 | 隔离进程 23 个 worker 模块成功启动 |
| 安装与 preflight 回归 | 修复后 36 项全部通过 |
| 新增及受影响逻辑回归 | 连接池、列表卸载、轮询、分享、上传及会话守卫定向测试通过；最后 WebDAV 3 个文件 76 项通过，两步验证 2 个文件 4 项通过 |
| 全量单元测试 | 修复后 638 个文件、5,350 项全部通过，用时 870.42 秒。此前唯一安装复制超时已消除；之后新增的两步验证与 WebDAV 变更另行定向复测通过，详见归档日志 |
| 浏览器业务流程 | 首轮 Chromium 44 项中 42 项通过；成本删除、两步验证已修正测试选择器并复测通过，文件完整操作、审计筛选及响应式均再次通过。新增 WebDAV 发现的两处真实问题已修复并通过最终端到端复测 |
| 跨浏览器公开页与登录后导航 | Chromium、Firefox、WebKit 共 15 项全部通过，包括失效会话回到登录页 |
| 响应式 | 46 个可直接访问页面 × 390/768/1440px 均通过两轮页面横向溢出和运行异常检查，另含 320px 分享文件名回归；已查看平板审计详情、窄屏分享及桌面仪表盘截图 |
| WebDAV HTTP 生命周期 | 最终通过：令牌创建与撤销、只读令牌不能删除、MKCOL、二进制 PUT/GET、206 Range、绝对 Destination MOVE、PROPFIND、DELETE；目录名含 Unicode、`%` 和 `_` |
| WebDAV SQL 实测 | 先用真实 PostgreSQL 复现有索引却返回空列表，再确认修复后普通目录、Unicode 与 LIKE 特殊字符目录均能返回直接子项；E2E 已纳入特殊目录名 |
| DAST | nuclei 未安装；轻量探测通过：公开页可达，受保护 API 返回 401，敏感路径未暴露 |
| 文档指标与补丁格式 | README 指标已更新，文档一致性与 diff 检查通过 |

业务浏览器检查涉及页面遍历、代码片段、公告、告警、命令模板、工单、API 令牌、计划任务、剧本、成本、用户、备份计划、文件上传与预览、公开分享下载、媒体与图床、设置、两步验证、通知、监控、流量、Docker 控件、下载任务、服务器诊断及团队工作区。

新增/修正测试文件包括 `e2e/responsive-layout.spec.ts`、`e2e/webdav-workflows.spec.ts`、`e2e/public-smoke.spec.ts`、`src/lib/__tests__/db.test.ts`、`src/lib/auth/__tests__/require-session.test.ts` 和两步验证表单测试。E2E 直接会话夹具补齐凭据指纹，并复用品牌 slug 规范化，避免测试 Cookie 与生产验证格式偏离。TOTP 测试等待剩余有效期充足再输入，并断言验证接口响应，避免在 30 秒窗口边缘产生随机失败。

## 验证边界与后续重点

- 首轮没有得到有效覆盖率汇总；延续运行 `vch-audit-coverage-continue2` 已正常完成并通过门槛：5,411 项通过、7 项跳过，语句 73.99%、分支 64.13%、函数 73.04%、行 76.79%。该报告之后的修改另有定向回归，详见工作记录。
- 页面横向不溢出和静态可访问性扫描不能证明全部弹窗、长文本、颜色对比和屏幕阅读器体验无问题。已结合代表页面截图与业务交互检查，但没有穷举所有数据组合。
- 外部真实 VPS/SSH、第三方 AI、云服务及远程 WebDAV 上游未使用生产凭据进行端到端连接；现有单测、合成不可达服务器及本地隔离服务覆盖了相关分支。
- **大文件库列表截断已在延续审查中修复**：首屏/API 共用数据库分页、节点/目录筛选、统计与有限分支树查询；共享选择器和回收站也接入分页。真实 PostgreSQL 7 项及 1,205 文件浏览器流程通过。旧概览接口的有限读取不能重新用于完整列表/计数。
- WebDAV 浅层同步已改用直接子项查询，本地同步减少重复更新并增加并发条件保护；真实远程上游和生产规模表现仍未进行外部端到端压测。
- 411 条 API 文案基线值得后续按模块收敛；本次保留既有基线，没有批量替换面向用户的错误语义。
- `npm audit` 无已知漏洞与轻量 DAST 通过不等于完成渗透测试。未进行生产负载压测或外网暴露扫描。

## 产物

- 源码与本报告：`/opt/VControlHub`。
- 隔离工作树和截图：`/tmp/vcontrolhub-audit-20260907`，浏览器产物位于其 `.playwright-output`。
- 构建、测试和扫描日志：`/tmp/vcontrolhub-audit-20260907-*.log`。
- 另行归档的成功日志和代表截图：`/root/vcontrolhub-audit-2026-09-07/logs`、`/root/vcontrolhub-audit-2026-09-07/screenshots`。
- 预览只使用隔离数据库和测试文件，不是线上已发布版本。
- 本机预览账号：`vcontrolhub_e2e`，密码 `VControlHub-E2E-2026!`。这是独立测试数据库中的临时管理员，不是生产账号。测试 worker 已停止；需要异步任务执行时可重新启动隔离 worker，前端与 HTTP 服务保持运行。

## 2026-09-08 延续结果（仍在审查）

- 共享主题令牌、语义组件样式与遗留兼容层分离；页面标题、区段、统计、表单、分页、侧栏、移动导航、弹窗焦点与键盘标签页进行统一。逐页迁移仍在继续，不能视为全站已经完成美化。
- 修复图床搜索竞态、末页删除、中文图片响应头、下载流无背压、文件内容搜索取消、并发本地同步、WebDAV 回收站恢复和团队成员超过 10 人无法展开管理等问题。
- 性能基准为 50,000 文件/100 目录的 5 次中位数：根列表 796 → 125.4ms，100 目录计数 2,150.3 → 251.4ms，完整列表服务 2,946.1 → 633.5ms。普通目录/搜索/统计未出现同级改善；这是隔离合成数据结果。
- 368 组页面扫描无检测到的溢出、裁切和浏览器异常；7 页 axe 问题修复后的 104 组复扫通过。填充文件、回收站/分享和图床真实位图工作流通过，证据分别位于 `populated-files`、`recycle-share`、`image-workflows`。移动菜单另有真实焦点/键盘/尺寸切换验证。
- `vch-audit-build-continue4` 编译及 TypeScript 通过，后续独立类型检查和 ESLint 通过。图床复制转义、Token 错误响应、重复操作与失败恢复已经定向验证；通知 WebSocket 的 URL 异常、关闭竞态和消息上限修复通过真实连接测试。
- 图床统计另发现 5,000 条截断，已用 6,001 条隔离记录复现并完成真实数据复测：修复前趋势增加 5,000，修复后增加 6,001。改为最近 7 个 UTC 自然日的带权限计数，三类权限范围测试通过。
- 最新全站视觉与 axe 共 368 组通过，图床/设置追加 16 组通过。菜单抽屉另修复底栏盖住抽屉的层级问题，真实命中和键盘回归通过。
- 图片预览重构为实色、内部关闭按钮、可收缩图片区及换行工具栏，修复桌面比例偏差与关闭按钮越界。真实位图业务流程通过；新增 3 种比例 × 5 视口 × 2 主题/语言的 30 次预览检查，通过比例、边界和焦点恢复断言并查看截图。
- 直接图片上传 Token 分支纳入公共错误守卫，修复验证和数据库异常逃逸；关联存储权限拒绝/查询失败后的四个残留文件已修复。配额锁改为 FileEntry 索引提交或失败回滚结束后释放。三类问题均先复现再修复，上传/列表/守卫 46 项回归通过。
- 第一轮业务回归 40/42 通过，隔离 worker 缺失与旧预览定位已处理；最新构建、完整业务重跑和跨浏览器检查进行中，以工作记录的最终更新为准。
- 继续复现 PNG 错命名为 `.webp`/`.avif` 时原图与转码输出同路径，导致字节及校验和不一致。内部存储扩展名改用解码格式，显示文件名保持不变。实际磁盘断言通过，上传/列表/下载/守卫现为 59 项通过；真实 HTTP 的错扩展名下载比对等待最终构建。
- 分片上传完成接口同步修复路径冲突、授权失败清理与配额锁时机，原图写失败时等待衍生任务结束再清理。新增五条原问题回归及一条延迟衍生文件回归，图床完整相关模块 11 文件 112 项全部通过。源码修改继续保留在工作区，尚未发布。
- 真实 Token 工作流进一步复现共享限流器返回过早的 Retry-After：拒绝请求也占窗口，却只等待最老请求到期。等待时间现按能容纳下一请求的过期位置计算，不修改预算或拒绝计数策略；真实内存存储的三个预算场景先复现失败，随后修复。`continue9` 业务 44/45 通过，剩余该问题正在最终构建与 HTTP 回归中验证。

## 本阶段最终结果

- `vch-business-e2e-continue10`：45 项全部通过，5.6 分钟；真实上传按 Retry-After 等待后成功，撤销 Token 后返回 401。直接与分片上传错扩展名的图片均逐字节下载一致。
- `vch-cross-browser-continue11`：Chromium、Firefox、WebKit 共 18 项通过。`vch-preview-cross-browser-continue12` 再次通过三浏览器共 90 组预览状态并复核截图。
- 预览矩阵使用受控列表/位图与无 Service Worker 夹具，避免浏览器替身被 Service Worker 绕过或出现人工阻止注册导致的告警。公开页和登录后烟测继续验证真实配置，不能将受控矩阵描述为真实后端覆盖。
- 全项目独立类型检查、构建、运行时打包、完整及最新定向 ESLint、文档指标和补丁格式检查均通过。先前全量覆盖率快照仍为 5,411 项通过；后续变更另行定向验证，不混写覆盖率百分比。
- 隔离预览与测试账号保留；临时剧本 worker 已停止，生产 Next、worker、SSH WebSocket 的启动时间仍为 2026-09-05 13:21:09 UTC。没有提交、推送或发布。
- 未完成领域及下一阶段检查对象见工作记录中的“后续审查范围”，包括全站遗留样式迁移、更多动态租户权限、真实远程上游和队列恢复场景。
