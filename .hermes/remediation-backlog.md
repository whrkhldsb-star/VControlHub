# VControlHub Autonomous Remediation Backlog

Created: 2026-05-29
Purpose: durable handoff for multi-round autonomous remediation and optimization. Each cron run must read this file first, choose a coherent batch, fix it, verify, deploy, commit/push, then update this file.

## Operating rules

- Do not rely on chat context. This file is the source of truth for remaining work.
- This remediation loop is distinct from the older paused general site-check loop:
  - Old paused job `a8e21e83a7df`: broad website issue discovery + small automatic fixes/upgrades.
  - Removed job `b8833e83bb1f`: previous backlog-driven remediation and optimization loop; stopped at user request on 2026-05-31 after watchdog cleanup.
  - Current job `9e36e64a75ae`: full-product QA remediation loop using the improved strategy below; it must cover real user workflows, feature completeness, code quality, performance, UI/UX/accessibility, reliability, deployment/runtime health, and side-effect verification.
- Runs are scheduled every 30 minutes under current job `9e36e64a75ae`, but must be non-overlapping. If the previous remediation run is still active, skip the new tick and report/record a skipped-overlap event instead of starting another fix.
- The half-hour remediation job now has a deterministic preflight script (`/root/.hermes/scripts/vcontrolhub_preflight.py`) attached through cron. Every run must reconcile that preflight before choosing work: git dirty/origin state, cron last_status/last_error, `vcontrolhub-next`/`vcontrolhub-ssh-ws`/`caddy`/`docker` services, Docker/Quick Services capability, `/api/status`, `/login`, `/quick-services`, `/api/quick-services`, `/files`, `/downloads`, `/operation-tasks`, and recent logs. Environment capability drift must be acted on immediately; for example, the old “Docker not installed” Quick Services blocker is stale once preflight proves Docker is installed and active, and the next appropriate canary must prove a real install/start/stop/uninstall lifecycle instead of reusing no-Docker evidence.
- The standalone lightweight watchdog cron `b2f7b030a16f` has been removed per user request. Do not recreate or schedule it unless explicitly asked. Its semantics are fused into the normal round closeout health check: perform a single unified check covering service active state, `/api/status` health, git worktree/origin status, critical cron states, and recent service logs. Do not run a duplicate watchdog pass after the same probes already succeeded; only fill missing checks.
- Keep fixes deployable, but avoid noisy GitHub commit churn. A remediation tick may verify or prepare a small fix, but GitHub commits/pushes should normally happen only after a coherent **major round** is complete: related functional fixes, validation, production verification, backlog/state update, and roadmap/future-goal update are bundled into one intentional commit. Exceptions are production breakage hotfixes, dirty-worktree closeouts from a prior run, or blockers where preserving a verified partial state is safer than waiting.
- Prefer functional availability and end-to-end correctness over cosmetic-only work, but UI/UX polish is valid when it improves usability.
- Add or update regression tests for behavior fixes where practical.
- Required validation before commit/push unless explicitly blocked: `npm run typecheck`, `npm run lint`, targeted tests, `npm run build`, `npm run build:runtime`, production restart of `vcontrolhub-next.service vcontrolhub-ssh-ws.service`, `./deploy/smoke-test.sh whrkhldsb.qzz.io vcontrolhub`, recent service log check.
- If production restart is blocked by approval policy, do not commit/push as deployed. Leave worktree verified but uncommitted, record blocker below, and report.
- After each completed item, mark it `[x]` with date, commit hash if available, and short verification notes.
- At every major-round closeout, perform a broad check before pushing: verify production health, major user-path coverage for that round, related tests, logs, git/origin sync, and whether the GitHub-visible future goals/roadmap docs need to change. If priorities shifted, update the relevant docs under `docs/plans/` in the same closeout commit rather than leaving GitHub's future-goal view stale.
- After the recommended current backlog is completed, or after roughly 6 successful remediation runs, perform a fresh comprehensive audit, update this checklist with newly discovered or reprioritized issues, update GitHub-visible future goals/roadmap docs, then continue remediation/optimization. When the checklist is completed, the audit must include a software-tester-style real browser/visual QA pass before any new remediation round starts: log into production with the browser tool, click through major pages, forms, dialogs, navigation, and core workflows from a real user perspective, visually inspect whether UI states and side effects are correct, and record/fix findings. This may be split into multiple QA batches, but the visual QA gate must be completed before starting the next ordinary backlog round.
- Routine successful runs should be silent to the user. Only send a user-facing summary after 6 successful remediation runs, when the backlog is basically completed/fresh audit is performed, or when a blocker/failure requires attention. Still update backlog/state every run.
- Future recreated “清单检查修复” tasks must not become architecture-only or static-scan-only loops. Each cycle should explicitly choose from: functional completeness, real user workflow availability, code quality/maintainability, performance/resource usage, reliability/observability, UI/UX/accessibility, permissions/security, tests, deployment/runtime health. Completion/fresh-audit cycles must include browser-driven production QA, not only route probes.
- Broad improvements are allowed: architecture, code, API design, UI, performance, reliability, feature completeness, observability, tests, deployment scripts, and docs/UX copy, as long as they improve the live website and pass verification.
- Settings page product direction: continue enriching Settings into the central customization/admin-control surface. Operators should be able to customize safe non-secret runtime behavior, UI/personal preferences, notification/channel behavior, polling/list limits, SSH/SFTP/command stability knobs, integrations such as AI/SMTP/image bed/API tokens where appropriate, feature toggles, display/theme/language defaults, and other product settings that currently require code/env edits. Every new setting must have a real runtime side effect, validation, clear immediate-vs-restart copy, tests, and production/user-path verification.

## P0 — Download task resource authorization

- [x] 2026-05-29 — Restrict `GET /api/downloads` so users only see owned tasks or tasks whose target storage path is readable. Verification: route regression `filters download list to owned tasks or readable storage targets`, targeted Vitest, typecheck, lint, build, runtime build, production smoke 19/19 passed.
- [x] 2026-05-29 — Restrict `PATCH /api/downloads` task actions (`pause`, `resume`, `refresh`, per-task speed) to task owners or callers with target storage write authority. Verification: cross-user forbidden control regression plus existing real-process-state regressions passed.
- [x] 2026-05-29 — Restrict `DELETE /api/downloads` cancel/delete to task owners or callers with target storage delete authority. Verification: cross-user forbidden cancellation regression plus existing cancellation side-effect regressions passed.
- [x] 2026-05-29 — Require `storage:manage-node` for `globalMaxSpeedKb` instead of only generic `storage:write`. Verification: global limit permission regression passed.
- [x] 2026-05-29 — Add route/service tests covering cross-user task visibility and forbidden cross-user control. Verification: `npx vitest run src/app/api/downloads/__tests__/route.test.ts` passed 16/16.

## P1 — Ticket permission model

- [x] 2026-05-29 — Split ticket permissions so normal authenticated users can create tickets without broad `ticket:manage`; added `ticket:create` and `ticket:read` alongside manager-only `ticket:manage`. Verification: targeted ticket/RBAC/seed Vitest 26/26, typecheck, lint, build, runtime build, seed, restart, production smoke 19/19 passed. Commit: c729ce5.
- [x] 2026-05-29 — Make manager/admin ticket list return all tickets; normal users only see created/assigned tickets via service-layer `includeAll`. Verification: route/service regressions passed.
- [x] 2026-05-29 — Keep ticket detail/comment access consistent for creator, assignee, and manager/admin; comments still require ticket participant or `ticket:manage`, not broad read-only access. Verification: id-route and list-route regressions passed.
- [x] 2026-05-29 — Update RBAC seed/tests for `ticket:create`, `ticket:read`, `ticket:manage`. Verification: `prisma/__tests__/seed.test.ts` and RBAC tests passed, `npx prisma db seed` ran successfully.
- [x] 2026-05-29 — Add regression tests for create/list/comment/update paths. Verification: `src/app/api/tickets/__tests__/route.test.ts`, `id-route.test.ts`, and `src/lib/ticket/service.test.ts` passed.

## P2 — Storage and file-size safety

- [x] 2026-05-30 — Add SFTP read/preview size limit in `src/app/api/storage/sftp-ops/route.ts`; large files now return HTTP 413 with a clear download-oriented error before remote read when indexed size is known, and after read as a fallback when index size is unknown. Verification: storage route regressions, neighboring storage tests, typecheck, lint, build, runtime build, production restart, smoke 19/19, route probes passed.
- [x] 2026-05-30 — Add local upload pre-read size checks using `File.size` before `arrayBuffer()`; uploads over 100 MB now return HTTP 413 before storage lookup, access checks, disk writes, or SFTP writes. Verification: regression proves `arrayBuffer()` is not called for oversized declared files; storage route regressions, typecheck, lint, build, runtime build, production smoke passed.
- [x] 2026-05-30 — Harden `src/lib/storage/access-control.ts` no-grant behavior to default deny for non-storage-managers; only `storage:manage-node` remains a break-glass bypass, with an explicit `VCONTROLHUB_STORAGE_GRANT_FALLBACK=true` compatibility flag for temporary legacy deployments. Verification: access-control regression proves old role-only fallback now fails, targeted storage tests 27/27 passed, typecheck, lint, build, runtime build, production restart, smoke 19/19, route probes returned 401 without 500.
- [x] 2026-05-30 — Add tests for over-limit SFTP read and over-limit local upload. Verification: `npx vitest run src/app/api/storage/sftp-ops/__tests__/route.test.ts src/app/api/storage/local/__tests__/route.test.ts src/lib/storage/__tests__/access-control.test.ts src/lib/storage/__tests__/sftp-sync.test.ts` passed 26/26.

## P3 — Quick Services permission and task robustness

- [x] 2026-05-30 — Replace Quick Services `user:manage` permission with `docker:manage` consistently in routes/UI/RBAC so Docker operators can manage Quick Services without broad user-management rights. Verification: Quick Services route regression proves `docker:manage` is used and `user:manage` is not; RBAC regression proves operators have Docker/Quick Services management without user management; targeted tests, typecheck, lint, build, runtime build, seed, production restart, smoke 19/19, and route probe passed. Commit: a36d768.
- [x] 2026-05-30 — Add seed/test synchronization for Quick Services permission labeling by expanding `docker:manage` labels to cover Quick Services. Verification: `prisma/__tests__/seed.test.ts` included in targeted run and `npx prisma db seed` executed successfully.
- [x] 2026-05-30 — Add per-process Quick Services lifecycle task lock and installing-state guard to prevent concurrent install/start/stop/uninstall operations from racing on the same slug. Verification: service regressions prove concurrent same-slug operations fail before DB/Docker side effects and uninstall refuses an installing service; targeted Quick Services/RBAC/seed Vitest 31/31, typecheck, lint, build, runtime build, production restart, smoke 19/19, route probe passed. Commit: 7b3c3ec.
- [x] 2026-05-30 — Improve Quick Services install failure visibility/logging and cleanup state: failed async Docker installs now record stderr/stdout-preferred Docker messages, mark the service `error`, and best-effort remove partial containers while preserving cleanup-failure context for operators. Verification: Quick Services service/API/client regressions 27/27, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/api/quick-services` and `/quick-services` probes returned 401/307 without 500. Commit: c729ce5.

## P4 — UI availability and polish

- [x] 2026-05-30 — Replace remaining internal notification dropdown `<a href>` navigation with Next.js `<Link>` so notification action items and the “查看全部通知” footer use client-side routing instead of full page reloads. Verification: notification bell regression 4/4, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/notifications` 307, `/api/notifications` 401, `/api/status` 200, recent logs clean. Commit: 04817c0.
- [x] 2026-05-30 — Replace mobile nav index references in `src/components/nav-items.tsx` with stable href-based selection and regression coverage for the intended bottom items (`/`, `/servers`, `/traffic`, `/files`, `/settings`). Verification: targeted mobile-nav/component tests, full Vitest suite 163 files / 640 tests, typecheck, lint, build, runtime build, production restart, smoke 19/19, route probes passed. Commit: 3bc6bbf.
- [x] 2026-05-30 — Replace `window.location.reload()` in announcement and ticket creation forms with App Router `router.refresh()` so successful submissions update server-rendered lists without a full page reload. Verification: new form regressions prove `router.refresh()` is called after successful CSRF POST; targeted tests, full Vitest suite, typecheck, lint, build, runtime build, production restart, smoke 19/19 passed. Commit: 3bc6bbf.
- [x] 2026-05-30 — Replace remaining `window.location.reload()` in two-factor settings with App Router `router.refresh()` so enable/disable refreshes server-rendered account state without a full page reload. Verification: two-factor/settings regressions 8/8, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/api/status` 200, `/settings` 307, 2FA setup unauth probe 401, recent logs clean. Commit: a095185.
- [x] 2026-05-30 — Add an actionable SFTP browser empty state when no SFTP nodes exist, and mount the SFTP remote browser on the Files page instead of leaving the remote browsing surface invisible. The empty state links to VPS onboarding and the storage-node manager anchor. Verification: SFTP/page/files-browser regressions 16/16, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/files` 307 and unauth SFTP API 401 without 500, recent logs clean. Commit: 3917ddc.
- [x] 2026-05-30 — Improve monitoring page error state with the backend/fetch error reason, a retry button for initial load failures, stale-data preservation plus an inline warning for refresh failures, and disabled loading labels during in-flight refresh/retry. Verification: monitoring page regressions 2/2, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/monitoring` 307 and `/api/monitoring/stats` 401 without 500, recent logs clean. Commit: b70edc3.
- [x] 2026-05-30 — Make Health dashboard auto-refresh respect global/user refresh preferences instead of fixed 30s; saved 60s/manual preferences now control the timer and the UI shows the active cadence/disabled state. Verification: health dashboard + refresh-preference regressions 10/10, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/health` 307, `/api/health` 401, `/api/status` 200, recent logs clean. Commit: efe1c4a.
- [x] 2026-05-30 — Improve light-theme compatibility for high-frequency monitoring/health/status surfaces by extending the global light-mode compatibility layer to cover warning/error/success/info tint backgrounds, borders, and high-contrast status text used by monitoring cards and operator alerts. Verification: monitoring CSS regression 3/3, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/monitoring` 307, unauth monitoring API 401, `/api/status` 200, recent logs clean. Commit: 1e4726a.

## P5 — Incomplete product surfaces

- [x] 2026-05-30 — Wire dashboard analytics API into the main dashboard with a client-side `DashboardAnalyticsPanel` showing 24h server resource bars, 7d download status trends, 30d audit activity, and 7d image-bed uploads, plus inline API failure handling. Verification: dashboard panel/page regressions 3/3, full Vitest 165 files / 652 tests, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/` 307, `/api/dashboard/analytics` 401, `/api/status` 200, recent logs clean. Commit: 86e21d2.
- [x] 2026-05-30 — Wire deployment export API into the Deployments page with a `deploy:export`-gated client panel that generates portable environment/systemd/Caddy/deploy-script templates through CSRF, previews returned files, and offers JSON download. Also aligned the POST schema with the current service contract so UI payloads no longer require unused legacy `format/services` fields. Verification: deploy-export route/page/service regressions 6/6, deployment neighboring tests 12/12, full Vitest 167 files / 656 tests, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/deployments` 307, `/api/deploy-export` 401, `/api/status` 200, recent logs clean.
- [x] 2026-05-30 — Clarify backup API behavior by making backup creation execute the real `deploy/backup.sh` artifact immediately: records now transition through RUNNING to COMPLETED/FAILED with real file size/error metadata, API/server-action callers use the same service path, and the UI/OpenAPI copy says “创建并执行” instead of implying a record-only success. Verification: backup service/API/page regressions 14/14, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/backups` 307, `/api/backups` 401, `/api/status` 200, recent logs clean.
- [x] 2026-05-30 — Add backup restore API/UI for `backup:restore`: completed backup rows now expose a guarded “执行恢复” action with explicit `RESTORE` confirmation, `POST /api/backups/[id]/restore` requires `backup:restore`, validates completed records and portable paths, checks the real artifact exists, then executes database restores through `scripts/restore-db.sh` with `CONFIRM_RESTORE=1` or FILES/FULL restores through `tar -xzf` without exposing secrets. Verification: backup service/API/page/client regressions 23/23, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/backups` 307, `/api/backups` 401, restore route unauth 401, `/api/status` 200, recent logs clean.
- [x] 2026-05-30 — Unify system-health API and Health page behavior: the Health page now passes the full system self-check report to the client, renders individual `/api/system-health` check rows alongside repair guidance, and refreshes the self-check data from the existing guarded API instead of showing only a server-rendered summary. Verification: health client/page/API/system-health regressions 21/21, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/health` 307, `/api/health` 401, `/api/system-health` 401, `/api/status` 200, recent logs clean.

- [x] 2026-06-04 — Harden one-click installer environment and proxy readiness: IP-only installs now skip Caddy and render/restart Apache, DESTDIR renders systemd/Caddy/Apache assets without host service-manager side effects, installer env overrides sync HOSTNAME/public origins/DATABASE_URL/runtime data paths, .env.example includes aria2 runtime settings, dashed APP_SLUG defaults produce PostgreSQL-safe identifiers, and SKIP_PACKAGES reverse-proxy failures are explicit. Verification: installer/aria2 regressions 33/33, deploy-assets, typecheck, lint, build, runtime build, DESTDIR IP-only dry run, production active + /api/status healthy.

- [x] 2026-06-04 — Fix login page light/day-mode readability: login page now uses default dark surfaces with `html.light` overrides for bright readable page/card/input/feature surfaces, preserves dark mode, exposes login errors via role=alert, and production browser login reaches the authenticated shell. Verification: login page tests 3/3, targeted lint, typecheck, build, runtime build, production restart, light/dark browser visual proof, real login redirect, smoke 24/24, /api/status healthy, recent logs clean after restart window.

- [x] 2026-06-07 — Close media library / image-bed consolidation: `/media?type=image` now exposes an image-bed workspace for batch image upload with storage-node/target-directory selection, existing media image cards can publish storage files to public image-bed links, image-bed search covers filename/path/album, and storage file helpers support LOCAL/SFTP read/write plus node-appropriate source links. Verification: media/image-bed/API/storage helper regressions 25/25, typecheck, targeted eslint, build/runtime/deploy smoke pending in this closeout.

- [x] 2026-06-07 — Strengthened media library / image-bed consolidation by making `/image-bed` the external-link management and source-audit surface while `/media?type=image` remains the preferred image workspace. The image-bed list API now includes source storage-node metadata, the page routes new image workflows back to the media image workspace, and each published image shows whether it came from direct upload or a LOCAL/SFTP media-library path. Verification: image-bed page + media service targeted regressions 10/10, `npm run typecheck`, `npm run lint -- --max-warnings=0`, `npm run verify` passed including full tests, Next build, runtime build, and deploy-assets; production restart, smoke 25/25, /api/status healthy, and recent service logs clean.

- [x] 2026-06-07 — Closed the image-bed delete authorization and CI hygiene slice. `/api/images/[id]` now allows deletion only for the image owner, `storage:delete`, or `role:manage`; `/api/images/batch` no longer treats `user:read` as delete authority and uses the same explicit management/delete boundary. Installer DESTDIR fake-root validation now prevents CI preflight from touching real runtime directories, and the text preview lint fix was corrected to avoid a duplicate initial editable-draft fetch. Verification: targeted Vitest for deploy preflight, single image delete, batch image delete, and text preview editable flow; `npm run verify` passed with typecheck, lint `--max-warnings=0`, full Vitest 203 files / 912 tests, Next build, runtime build, deploy-assets; production restart; smoke 25/25; `/api/status` healthy; recent service logs clean.

- [x] 2026-06-07 — Closed README P0 local text-editor storage grant bypass: `/api/files/editable/[id]` now passes the authenticated session into editable draft reads/saves, and `src/lib/storage/service.ts` checks `assertStorageAccess` against the resolved LOCAL storage node and relative path before reading or writing. Saves include the new content byte count so path grants, per-file limits, and quota checks apply to browser edits. Verification: targeted editable-route/storage regressions 22/22, typecheck, lint `--max-warnings=0`, and `npm run verify` passed including full tests, Next build, runtime build, and deploy-assets; production restart, smoke 25/25, /api/status healthy, and recent service logs clean.

- [x] 2026-06-07 — Closed the media visual workspace and image-link-center redesign. `/media` now presents a visual media workbench with type-specific cards and recommended flows, media cards render image thumbnails through `/api/media/:id/stream`, video cards render first-frame stream covers, and audio cards use a matching visual cover; image mode highlights the publishing workflow while `/image-bed` is now the “图片外链中心” focused on copied links, source audit, cloud-path publishing, batch management, and a folded legacy direct-upload compatibility entry. The shared Node-to-Web stream adapter was hardened so client-aborted media/download streams do not produce `Controller is already closed` uncaught exceptions. Verification: media/image-bed targeted regressions 12/12, affected media/image-bed/media-stream/storage-stream regressions 29/29, `git diff --check`, full `npm run verify`, production restart, smoke 25/25, `/api/status` healthy, media-stream abort probe, and post-restart logs clean.

## P6 — Architecture hardening

- [ ] Move command/deployment SSH execution out of synchronous API path into background tasks/worker model with timeouts, output limits, cancellation, and task status.
- [x] 2026-06-08 — Stage 12 SSH terminal mobile layout: the SSH terminal modal now uses a scrollable small-screen shell, wraps the header action row, stacks the command panel full-width on mobile while preserving the 64-width side panel on desktop, and replaces the fixed 400px terminal minimum with `clamp(320px,58vh,560px)` plus desktop `lg:min-h-[400px]`. Targeted verification: SSH terminal modal regressions passed 4/4 plus typecheck passed. Remaining follow-ups: visible labels for remaining placeholder-only controls, especially text preview search/jump.
- [x] 2026-06-08 — Stage 11 SSH terminal dialog focus convergence: SSH terminal modal now reuses the shared `useDialogFocus` hook for initial close-button focus, Escape close, Tab/Shift+Tab focus containment, and trigger focus restoration after close. The favorite-command input now has an explicit accessible label instead of relying only on placeholder text. Targeted verification: SSH terminal modal regressions passed 3/3 plus typecheck passed. Remaining follow-ups: mobile terminal layout and visible labels for remaining placeholder-only controls.
- [x] 2026-06-08 — Stage 10 file browser history navigation: directory-tree and breadcrumb folder navigation now use `history.pushState` for real `/files?path=...&nodeId=...` history entries, while refresh/upload/create-folder/search in-place updates continue to use `replaceState` so they do not pollute the back stack. The Files SPA also listens for `popstate`, restores path/search/scope/nodeId from the current URL, reloads `/api/files/list`, and avoids rewriting history during browser back/forward restoration. Targeted verification: FilesBrowserSpa regressions passed 10/10 plus typecheck passed. Remaining follow-ups: SSH terminal dialog/mobile accessibility and remaining placeholder-only labels.
- [x] 2026-06-08 — Stage 9 unified Dialog Focus base for Docker logs: added a reusable `useDialogFocus` client hook for initial focus, Escape close, Tab/Shift+Tab focus wrapping, and trigger focus restoration. Docker container logs now open in a labelled `role="dialog"` with `aria-modal`, named close button, light/dark readable log surface, Escape close, and focus restore; the Docker remove confirmation dialog also uses the same focus-management base. Verification: git diff --check passed; Docker page regressions passed 4/4; typecheck passed; lint passed with `--max-warnings=0`; full `npm run verify` passed including Vitest, Next build, runtime build, and deploy-assets; production services restarted/reloaded, `/api/status` healthy 3/3, smoke 25/25, and recent next/ssh-ws logs clean. Remaining follow-ups: wire the same hook into the SSH terminal modal, improve mobile terminal layout, and continue visible labels for remaining placeholder-only controls.
- [x] 2026-06-08 — Stage 8 public share archive streaming: public directory share pages now expose a visible `下载整个目录` action backed by `/api/share/:token?archive=1`, streaming a tar.gz archive for LOCAL and SFTP directory shares while preserving existing `?path=` child-file downloads. The authenticated `/api/storage/archive-download` route and public share archive route now reuse a shared storage archive helper for safe RFC 5987 archive filenames, `application/gzip`/private headers, LOCAL/SFTP tar streaming, and SSH client cleanup on stream close/error/end. Targeted verification: authenticated archive-download and public share route regressions passed 7/7 plus typecheck passed. Remaining follow-ups: migrate media stream range/header code into the shared streaming helper and document/extend controlled archive extraction boundaries.
- [x] 2026-06-08 — Stage 7 file inventory consistency: soft delete now marks the `FileEntry` recycle-bin state before best-effort LOCAL/SFTP backing-object deletion, so an indexing failure can no longer leave an already-deleted physical object visible as active metadata. If backing deletion fails after the DB transition, the action returns a recoverable warning and emits `storage.file_delete_backing_failed` audit detail instead of rolling the UI back into an unsafe active state. Restore actions now call the storage service `restoreFileEntry` path, which verifies LOCAL/SFTP backing existence and type before clearing `isDeleted`. Targeted verification: storage actions and storage service regressions passed 31/31 plus typecheck passed. Remaining follow-ups: scheduled stale inventory reconciliation for out-of-band SFTP deletes and pagination/aggregation bounds for very large file indexes.
- [x] 2026-06-08 — Stage 6 Storage Stream unification: extracted a shared storage stream helper for byte-range parsing, private download headers, RFC 5987 content-disposition, and Node-to-Web response conversion; `/api/storage/local` now uses the helper for full and ranged LOCAL downloads, and `/api/storage/sftp-download` now supports `Range` / `206` / `416` with shared headers while still closing the SSH client on stream close/error and invalid range responses. Targeted verification: storage stream helper, local route, and SFTP download route regressions passed 20/20 plus typecheck passed. Remaining follow-ups: migrate `/api/media/[id]/stream` off its local duplicate range/header helpers and add public directory share archive streaming.
- [x] 2026-06-08 — Stage 5 runtime boundary disclosure: QuickService access URL generation now returns a structured descriptor instead of a bare string, classifying entries as `direct-port` or `reverse-proxy`; the `/quick-services` running overview and installed cards label direct host-port access as “公开直连端口” and expose a title/accessible-name warning that those links bypass VControlHub login and require firewall/VPN/app-level auth before exposure. Targeted verification: QuickService access URL descriptor regressions and QuickServices client regressions 13/13 plus typecheck passed. Remaining follow-ups: Docker socket privilege model/operator docs, QuickService durable install/update jobs with rollback/history, and Direct Gateway default TLS/VPN/firewall deployment hardening.
- [x] 2026-06-08 — Stage 4 AI Hosted Tools authorization hardening: hosted SSH execution now requires a session context with `server:ssh` inside `hosted-service` instead of relying on one chat-route branch, so auto-approved tools fail closed from any service caller; hosted action approval/rejection now requires `ai:action:approve` and no longer lets the original requester self-approve dangerous tool calls; approved execution rechecks the approver's `server:ssh` before opening SSH. `/api/ai/hosted-actions/[id]` now uses the shared guard for `ai:action:approve`. Targeted verification: hosted approval/service/API guard regressions 13/13 plus typecheck passed. Remaining follow-ups: server ownership/per-node authorization, read-log directory whitelist, and richer approval audit reporting.
- [x] 2026-06-08 — Stage 3 backup/restore durable jobs and real deployment rollback: backup creation and restore now default to durable-job enqueue (`backup.create` / `backup.restore`) with synchronous `?wait=1` compatibility, restore validates backup existence before queueing, and the custom server starts the backup job worker alongside SFTP jobs. Deployment rollback is now snapshot-backed instead of “resend deploy”: command templates can store a `rollbackCommand`, each deployment run persists an immutable `DeploymentSnapshot`, and `/api/deployments/:id/rollback` creates a `DeploymentRollbackRun` that submits the snapshot rollback command through the existing approval/execution pipeline. Verification target: backup route regressions, restore route regressions, deployment service rollback snapshot regression, command-template/deployment UI regressions, typecheck, full verify, production restart, smoke, and log scan.
- [x] 2026-06-08 — Stage 2 SFTP sync background migration: `POST /api/storage/sftp-sync` now defaults to durable-job enqueue (`202` with `jobId`/`taskId`) instead of scanning remote directories inside the request, while `?wait=1` preserves the legacy synchronous response path for compatibility. The custom server starts an idempotent SFTP sync worker that claims `storage.sftp-sync` jobs, refreshes heartbeats, runs the existing sync service, completes jobs with result payloads, and retries no-progress failures through the shared durable job lease/retry path. Verification: targeted SFTP sync route/worker plus durable job and operation-task regressions 15/15, `git diff --check`, full `npm run verify` passed with 211 files / 971 tests plus Next build/runtime build/deploy-assets, production restart, `/api/status` healthy 3/3, smoke 25/25, SFTP worker startup log proof, and recent logs clean.
- [x] 2026-06-08 — Stage 1 unified Durable Job queue base: added the DB-backed `jobs` table and `JobStatus`, plus reusable enqueue/lease-claim/heartbeat/complete/fail-retry/cancel/stale-recovery service helpers. Operation Tasks now aggregates durable jobs alongside commands/downloads/sync/backups/deployments so future migrated long tasks have a visible owner/progress/heartbeat path. Verification: `npm run prisma:generate`, durable job + operation-task targeted regressions 9/9, and `npm run typecheck` passed. Remaining: migrate backups/restore/SFTP/QuickService/download/alert execution into this queue and add concurrency limits/workers.
- [x] 2026-05-30 — Stage 1 command/deployment execution decoupling: approved/direct command requests now transition to RUNNING, mark targets RUNNING, write an enqueue log, and return to the caller before SSH target execution finalizes asynchronously in-process. This removes long SSH waits from command/deployment API and approval paths while preserving real target status/log updates; full durable worker cancellation/timeouts remain as the next architecture slice. Verification: command service regression proves user and approval flows enqueue without spawning SSH before the caller returns, neighboring deployment/operation-task/commands API tests 17/17, typecheck, lint, build, runtime build, production restart, smoke 19/19, route probes passed.
- [x] 2026-05-30 — Stage 2 command/deployment SSH execution guardrails: background SSH execution now enforces a default 5-minute per-target timeout, kills timed-out local ssh/sshpass processes, marks the target/request failed with exit code 124, and bounds captured stdout/stderr to 256 KiB (configurable via `COMMAND_EXECUTION_TIMEOUT_MS` and `COMMAND_OUTPUT_LIMIT_BYTES`) so chatty or hung commands cannot exhaust API worker resources. Verification: command service timeout/output-limit regressions 10/10, neighboring deployment/operation-task/commands API tests 17/17, typecheck, lint, build, runtime build, production restart, smoke 19/19, route probes passed.
- [x] 2026-05-30 — Stage 3 command/deployment task reconciliation: command list reads now first recover stale `RUNNING` command requests older than the execution window (configurable via `COMMAND_STALE_RUNNING_AFTER_MS`), failing queued/running targets that likely lost their in-process worker after service restart or archiving requests whose targets had already reached terminal state. This prevents Operation Tasks/Commands from being stuck forever after a process restart while the durable worker/cancellation architecture remains open. Verification: stale RUNNING recovery and route regressions 19/19, neighboring deployment/operation-task tests 11/11, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/api/commands` 401, `/operation-tasks` 307, `/api/status` 200, recent logs clean.
- [x] 2026-05-30 — Stage 4 command cancellation control: added `PATCH /api/commands` cancel action guarded by `command:execute`; cancelling a pending/approved/running command now marks active targets and the aggregate request `CANCELLED`, writes an execution log/audit entry, and terminates any in-process SSH child for running targets with exit code 130. Verification: command service/API cancellation regressions plus existing command tests 22/22, neighboring deployment/operation-task tests 11/11, typecheck, lint, build, runtime build, production restart, smoke 19/19, route probes passed.
- [x] 2026-05-30 — Stage 5 command maintenance reconciliation worker: the custom Next.js server now starts an idempotent command maintenance worker that reconciles stale `RUNNING` command requests on startup and every minute (configurable via `COMMAND_RECONCILE_INTERVAL_MS`) without waiting for an operator to open the Commands API/list page; instrumentation can opt in for alternate runtimes via `VCONTROLHUB_START_COMMAND_WORKER_IN_NEXT=true`. Verification: worker/service/API command regressions 24/24, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/api/status` 200, `/api/commands` 401, `/operation-tasks` 307, recent logs clean.
- [x] 2026-05-30 — Stage 6 command/deployment execution performance slice: background SSH target execution now runs targets concurrently with per-target timeout/output/cancellation guardrails preserved, so one slow VPS no longer blocks other targets from starting or completing. Verification: concurrent target regression plus command worker/service/API, deployment, and operation-task neighboring tests 36/36; full Vitest 171 files / 691 tests; typecheck; lint; build; runtime build; production restart; smoke 19/19; `/api/status` 200, `/api/commands` 401, `/operation-tasks` 307, `/deployments` 307; recent logs clean. Commit: 37e79a7.
- [x] 2026-05-30 — Continue P6 durable command/deployment background-worker architecture: the command maintenance worker now recovers `APPROVED` command requests left queued after a prior process exits before scheduling execution, atomically claims them with `updateMany`, marks targets running, logs the re-enqueue, and starts background SSH execution without duplicating already-claimed work. Verification: command queued-recovery regressions plus command/deployment/operation-task neighboring tests 36/36, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/api/commands` 401, `/operation-tasks` 307, `/deployments` 307, `/api/status` 200, recent logs clean. Commit: 39e3621.
- [x] 2026-05-30 — Continue P6 durable command/deployment background-worker architecture: add RUNNING command execution heartbeats so long-lived background SSH work refreshes `CommandRequest.updatedAt` while active and is not falsely recovered as stale before its timeout window. The command maintenance worker tests now also cover queued APPROVED recovery, preventing a silent regression where startup/interval reconciliation skipped the approved queue path. Verification: command service/worker regressions 20/20, neighboring deployment/operation-task/commands API tests 20/20, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/api/commands` 401, `/operation-tasks` 307, `/deployments` 307, `/api/status` 200, recent logs clean. Commit: 5d59440.
- [x] 2026-05-30 — Continue P6 durable command/deployment background-worker architecture: persist worker ownership metadata (`workerId`, `workerHeartbeatAt`) on RUNNING command requests, refresh it during background SSH execution, clear it on terminal states/cancel/failure, and make stale recovery prefer worker heartbeat freshness with an `updatedAt` fallback for legacy rows. This makes restart-surviving queue claims more observable and prepares richer worker/task reconciliation beyond process-local state. Verification: command service/worker regressions 20/20, neighboring deployment/operation-task/commands API tests 20/20, prisma generate/db push, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/api/commands` 401, `/operation-tasks` 307, `/deployments` 307, `/api/status` 200, recent logs clean. Commit: 53e5331.
- [x] 2026-05-30 — Continue P6 durable command/deployment background-worker architecture: add operator-visible worker ownership/heartbeat diagnostics into Task Center aggregation and UI. Command/deployment tasks now carry `workerId`/`workerHeartbeatAt`, the Operation Tasks page renders the owning worker badge and heartbeat context, and deployment task aggregation selects the linked command worker metadata so restart-surviving queue claims are visible to operators instead of hidden in raw DB fields. Verification: operation-task service/client regressions 3/3, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/api/operation-tasks` 401, `/operation-tasks` 307, `/requests` 307, `/deployments` 307, `/api/status` 200, recent logs clean. Commit: 47f159b.
- [x] 2026-05-30 — Continue P6 durable command/deployment background-worker architecture: add richer restart-surviving queue claim diagnostics/reconciliation history beyond Task Center worker ownership display. Stale RUNNING recovery logs now include worker ownership/heartbeat details, queued APPROVED recovery logs identify the claiming worker, and the Requests approval page renders recent execution/worker history instead of only the latest log. Verification: command service + Requests page + Operation Tasks neighboring regressions 21/21, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/api/commands` 401, `/requests` 307, `/operation-tasks` 307, `/deployments` 307, `/api/status` 200, recent logs clean. Commit: 411cb50.
- [x] 2026-05-30 — Reduce command API auth-route drift by migrating `/api/commands` GET/POST/PATCH from ad-hoc `requireAuth` + manual permission branches to declarative `withApiRoute({ permission })` guards while preserving the execute-permission downgrade-to-approval behavior for command submission and the `command:execute` boundary for cancellation. Verification: command route/service/worker regressions 25/25, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/api/commands` 401, `/operation-tasks` 307, `/api/status` 200, recent logs clean. Commit: bd9e89a.
- [x] 2026-05-30 — Strengthen download URL SSRF protections by resolving DNS before task creation and rejecting hostnames whose DNS answers point at private, loopback, link-local, multicast, or metadata ranges. Download creation now fails before server lookup, storage grants, DB writes, or remote SSH side effects when a public-looking hostname resolves internally. Verification: source-url and downloads route regressions 25/25, neighboring remote-command tests 4/4, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/api/downloads` 401, `/downloads` 307, `/api/status` 200, recent logs clean. Commit: 066fc31.
- [x] 2026-05-30 — Unify API rate limiting onto the shared rate-limit store so guarded API/file-upload/file-proxy rate checks use Redis when `REDIS_URL` is configured, while legacy login form checks keep their synchronous local lockout-compatible path. Added the runtime `redis` dependency, made `withRateLimit` async, and covered the store delegation with regressions. Verification: rate-limit/API-guard regressions 9/9, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/api/images/upload` 401, `/api/servers/nonexistent/file-proxy` 401, `/api/status` 200, recent logs clean.
- [x] 2026-05-30 — Replace remaining local shell-string `execSync` health/Quick Services port checks with argv-based `execFileSync` execution and in-process parsing. System Health now calls `systemctl`/`git` without a shell pipeline, and Quick Services port discovery/check-port no longer shells through `grep`/pipes. Remaining `ssh2.exec(...)` uses are remote-command execution surfaces tracked separately by the background-task hardening item. Verification: system-health + quick-service regressions 19/19, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/api/status` 200, `/api/system-health` 401, Quick Services check-port unauth 401, recent logs clean.
- [x] 2026-05-30 — Bound Quick Services catalog/list reads for API scalability: installed Quick Services listing now has a 200-row cap, remote app catalog hydration returns at most 500 enabled apps, and enabled app-source sync enumerates at most 50 sources in deterministic creation order. Verification: app-source/Quick Services API/service regressions 26/26, full Vitest 172 files / 693 tests, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/quick-services` 307, `/api/quick-services` 401, `/api/status` 200, recent logs clean.
- [x] 2026-05-30 — Migrate `/api/health` cookie-session authorization to the shared declarative API guard with `permission: "health:read"` while preserving fail-closed Bearer-token handling for external monitors. Verification: health route regression proves `requireApiPermission("health:read")` short-circuits before health collection, targeted health tests 8/8, typecheck, lint, build, runtime build, production restart, smoke 19/19 after transient retry, `/api/health` and history probes 401, `/health` 307, `/api/status` 200, recent logs clean.

## Fresh-audit checkpoint policy

- [x] 2026-05-30 — Fresh audit checkpoint after 7 successful remediation runs: git clean at start, production services active, smoke 19/19 passed, key authenticated APIs returned 401 instead of 500, recent logs contained no new error/warn entries, full quality gate passed (`typecheck`, `lint`, full Vitest 163 files / 640 tests, `build`, `build:runtime`). Backlog was refreshed; remaining priority is functional availability for Quick Services failure cleanup, two-factor settings no-reload UX, storage/monitoring/health usability, incomplete dashboard/deploy/backup surfaces, and architecture hardening.
- [x] 2026-05-30 — Fresh audit checkpoint after 7 additional successful remediation runs: production services active before work, `/api/status` healthy, no recent service error/warn matches, `typecheck`, `lint`, full Vitest 167 files / 656 tests, `build`, `build:runtime`, production restart, smoke 19/19, and key unauth route probes returned 307/401/200 instead of 500. Backlog refreshed; remaining P5 priority is backup behavior/restore and Health/system-health unification, followed by P6 architecture hardening.
- [x] 2026-05-30 — Fresh audit checkpoint after 6 additional successful remediation runs: git clean, no overlapping remediation/build/test process, production `vcontrolhub-next.service` and `vcontrolhub-ssh-ws.service` active, `/api/status` healthy, major HTML/API probes returned 307/401/200 instead of 500, recent service logs had no error/warn matches, and full quality gate passed (`typecheck`, `lint`, full Vitest 170 files / 675 tests, `build`, `build:runtime`, smoke 19/19). Backlog refreshed; the only carried-over known item is the larger P6 command/deployment background-task architecture hardening, and the next audit/repair pass should look for a smaller deployable slice around async task execution, Quick Services/app-source scaling, storage list bounds, or newly discovered UI/API gaps.
- [x] 2026-05-30 — Fresh audit checkpoint after 7 additional successful remediation runs: no overlapping remediation/build/test process, git clean, production `vcontrolhub-next.service` and `vcontrolhub-ssh-ws.service` active, `/api/status` healthy, major HTML/API probes returned 307/401/200 instead of 500, recent service logs had no error/warn matches, and full quality gate passed (`typecheck`, `lint`, full Vitest 172 files / 693 tests, `build`, `build:runtime`, smoke 19/19). Backlog refreshed; remaining high-value work is P6 durable command/deployment background-worker architecture (restart-surviving queue claims/worker ownership/richer task reconciliation) plus newly discovered smaller audit candidates: reduce remaining manual API auth route(s), review bounded `findMany` candidates where cardinality can grow, and replace remaining internal `<a href>` with `Link` where appropriate.
- [x] 2026-05-30 — Fresh audit checkpoint after 6 additional successful remediation runs: no overlapping remediation/build/test process, git/remote clean at start, production `vcontrolhub-next.service` and `vcontrolhub-ssh-ws.service` active, `/api/status` healthy, major HTML/API probes returned 307/401/200 instead of 500, recent service logs had no error/warn matches, and full quality gate passed (`typecheck`, `lint`, full Vitest 172 files / 698 tests, `build`, `build:runtime`, smoke 19/19). Backlog refreshed; remaining high-value work is the final P6 durable command/deployment background-worker slice for operator-visible worker ownership/heartbeat diagnostics and richer restart-surviving task reconciliation, followed by a new comprehensive audit once that current checklist item lands.
- [x] 2026-05-30 — Fresh audit checkpoint after 10 additional successful remediation runs: no overlapping remediation/build/test process beyond editor TypeScript servers, git/remote clean at start, production `vcontrolhub-next.service`, `vcontrolhub-ssh-ws.service`, and `caddy.service` active, `/api/status` healthy, major HTML/API probes returned 307/401/200 instead of 500, recent service logs had no error/warn matches, and the full verification baseline was healthy (`typecheck`, `lint`, full Vitest 176 files / 724 tests, `build`, `build:runtime`, smoke 19/19). Static audit reconfirmed no remaining internal app `<a href="/` navigation, no shipped `confirm(`/`prompt(`/`window.location.reload()` calls under `src/app`, and no obvious user-path production 500s. Backlog refreshed; the carried high-value item remains P1 background/task UX for long-running storage/SFTP sync/import work, and newly rechecked performance candidates include bounded `findMany` cleanup for growing list surfaces such as sync/share/backup/AI history where appropriate.
- [x] 2026-05-31 — Fresh audit checkpoint after 6 additional successful remediation runs: no overlapping remediation/build/test process, git/remote clean at start, production `vcontrolhub-next.service`, `vcontrolhub-ssh-ws.service`, and `caddy.service` active, `/api/status` healthy, major HTML/API probes returned 307/401/200 instead of 500, recent service logs had no error/warn matches, and the full verification baseline was healthy (`typecheck`, `lint` with existing client-effect warnings only, full Vitest 178 files / 734 tests, `build`, `build:runtime`, smoke 19/19). Static audit reconfirmed no remaining internal app `<a href="/` navigation and no shipped `confirm(`/`prompt(`/`window.location.reload()` calls under `src/app`. No code remediation was needed in this checkpoint run; success counter was reset and the next loop should continue from current audit candidates: bounded growing-list surfaces, API auth-route drift, long-running storage/SFTP background task UX, and newly discovered product gaps.
- [x] 2026-05-31 — Fresh audit checkpoint after 6 additional successful remediation runs: no overlapping remediation/build/test process, git/remote clean at start, production `vcontrolhub-next.service`, `vcontrolhub-ssh-ws.service`, and `caddy.service` active, `/api/status` healthy, login returned 200 and major guarded HTML/API probes returned 307/401/200 instead of 500, recent service logs had no error/warn matches, static scans reconfirmed no internal app `<a href="/` navigation and no shipped `confirm(`/`prompt(`/`window.location.reload()` calls under `src/app`, and the full verification baseline was healthy (`typecheck`, `lint` with existing client-effect warnings only, full Vitest 181 files / 757 tests, `build`, `build:runtime`, restart, smoke 19/19). The audit found and fixed one stale Operation Tasks API route regression expectation after the runtime-configurable list-limit change; production behavior was already bounded in the service. Commit: 04b7675.
- [x] 2026-06-08 — Fresh audit checkpoint after the run counter reached the overall-review threshold: git clean and HEAD == origin/main `9a0e418`; Docker available/running; production `vcontrolhub-next`, `vcontrolhub-ssh-ws`, `caddy`, and `docker` services active/enabled; `/api/status` healthy 3/3; no overlapping build/test/deploy processes; key public/protected route matrix returned expected 200/307/401 responses; `./deploy/smoke-test.sh whrkhldsb.qzz.io vcontrolhub` passed 25/25; recent next/ssh-ws logs had no error/warn/failed/exception matches; no `qs-*` Quick Services containers were left behind. README remaining boundaries were reviewed and no safe small code change was opened in this checkpoint; next concrete candidates remain durable job/lease for long tasks, request-bound backup/SFTP work, true deployment rollback, AI Hosted Tools authorization, Docker/QuickService/Direct Gateway boundaries, SFTP Range/share archive, file-state/list-performance, and accessibility/mobile/navigation closure.
  - [ ] QA batch 1 — Login, shell/navigation, dashboard, global search, notifications, preferences/default-page behavior, and basic responsive layout. Verify visual states, loading/error/empty states, redirects, and that preference changes create real app-side effects.
    - [x] 2026-05-31 — Started QA batch 1 with production browser login and dashboard/shell visual inspection. Login rendered cleanly; authenticated dashboard mounted without 500; visual QA found sidebar/footer crowding for long usernames and possible navigation overflow; fixed sidebar width, horizontal clipping, and username truncation/title behavior. Remaining QA batch 1 work: global search, notifications dropdown interactions, preferences/default-page side effects, and responsive/mobile layout.
    - [x] 2026-05-31 — Continued QA batch 1 with production browser login and language-toggle visual inspection. Found the dashboard analytics panel stayed in Chinese after switching the shell to English, making the localized dashboard visibly inconsistent; fixed analytics labels, loading/error/empty states, metric labels, and chart aria labels to consume the active locale. Verification: dashboard analytics regression 3/3, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart, smoke 19/19, route probes/log checks clean. Commit: 830e4ac.
    - [x] 2026-05-31 — Continued completion-gate QA by exercising the AI entry path from an authenticated production browser session. Verified `/ai` empty state and no-provider “+ 新对话” flow; found the AI provider setup overlay was visually present but lacked dialog semantics, so screen-reader users could not identify it as the active modal. Added `role="dialog"`, `aria-modal`, and labelled title wiring plus a regression proving no conversation POST occurs without an enabled provider and the setup dialog is accessible. Verification: AI client regression 5/5, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart with .next ownership repair, smoke 19/19, `/ai`/AI provider/API probes without 500, browser DOM proof of labelled dialog, recent logs clean. Commit: 57f9922.
    - [x] 2026-05-31 — Continued QA batch 1 by exercising the production notification dropdown in dark and light themes. Found the light-theme dropdown used an inconsistent mixed dark/light surface with weak empty-state contrast and lacked explicit popover/dialog semantics; added trigger popover attributes, labelled `role="dialog"` semantics, Escape-to-close behavior, semantic notification list markup, and light-theme contrast classes. Verification: notification bell regression 7/7, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart with `.next` ownership repair, smoke 19/19, `/notifications` and `/api/notifications` probes without 500, browser DOM proof of labelled notification dialog, recent logs clean. Commit: c4933ab.
    - [x] 2026-05-31 — Continued QA batch 1 login/default-page user-path verification. Production browser QA proved an already-authenticated operator could navigate directly to `/login` and see the login form, but protected authenticated chrome (sidebar, mobile navigation, global search, user controls) still rendered around it because the public-auth marker was not forwarded into server component request headers and `SidebarLoader` was always mounted. Fixed the proxy to forward `x-vcontrolhub-public-auth-page` on the request, gated sidebar/mobile/global-search chrome behind the same predicate, and added proxy/layout regressions. Verification: proxy/layout regressions 10/10, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart, smoke 19/19, browser login→dashboard→direct `/login` proof showed no sidebar/nav/global-search chrome, `/login` and `/login/verify-2fa` 200, `/api/status` 200, recent logs clean. Commit: f635ad5.
    - [x] 2026-05-31 — Continued QA batch 1 preferences/default-page side-effect verification. Production browser QA exercised the real `/api/preferences` save path and confirmed a successful default-page change persisted to DB/local preference state and redirected the next login to `/files`; it also proved a failed write (missing CSRF) returned 403 without changing the visible stored preference. Found the client still wrote `localStorage` and fired `vps-preferences-updated` before the server confirmed the save, so other tabs/widgets could react to a preference that never persisted if the PUT failed. Fixed Preferences to publish local side effects only after the API save returns effective normalized values, and added regressions for failed-save no side effect plus success-save event publication. Verification: preferences/API/login/dashboard preference regressions 12/12, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart, smoke 19/19, `/preferences` 307, `/api/preferences` 401, `/login` 200, `/api/status` 200, recent logs clean, QA account cleanup verified 0 remaining. Commit: 8155f73.
    - [x] 2026-06-08 — Continued QA batch 1 notification-center accessibility/mobile closeout. Change Contract: an operator using `/notifications` on mobile or with keyboard navigation must be able to discover and activate destructive/read actions without relying on desktop hover, and light mode must keep notification title/body/time/status text readable. Fixed the notification list so delete is visible on small screens, becomes visible on desktop `group-focus-within`, cards show focus rings, action buttons expose focus-visible rings, decorative icons are hidden from assistive tech, and light-theme card/read/unread text/borders are explicit. Verification target: `src/app/notifications/__tests__/notification-list-client.test.tsx` now proves destructive actions stay keyboard/small-screen reachable.
    - [x] 2026-05-31 — Continued completion-gate/fresh-audit health QA by converging a prior dirty Health Center localization fix instead of opening a new topic. The Health/System Health client now consumes the active language for nested self-check headings, repair cards, status badges, overview controls, table labels, trend labels, and common Chinese service/API messages returned by `/api/system-health`, so English mode no longer shows a mixed Chinese/English Health Center. Verification: health client/page/system-health regressions 14/14, `git diff --check`, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart with `.next` ownership repair, smoke 19/19, `/health` 307, `/api/health` 401, `/api/system-health` 401, `/login` 200, `/api/status` 200, recent logs clean. Commit: 3ea9b17.
    - [x] 2026-06-08 — Continued QA batch 1 / QA batch 4 Downloads closeout from a real production browser session. Found the authenticated shell exposed no visible global-search entry while relying on an unreliable hidden/shortcut-only path, so the sidebar now has a first-class “全局搜索” control that dispatches the existing search dialog; `GlobalSearch` also accepts external open signals and listens for the shared `vcontrolhub:open-global-search` event. Downloads lifecycle coverage was expanded with a purge-history matrix proving terminal task record deletion does not touch remote processes and active tasks must be cancelled first. Verification: targeted search/sidebar/downloads regressions 45/45, `npm run typecheck`, `npm run build`, `npm run build:runtime`, full `npm run verify` passed with 209 test files / 960 tests plus build/runtime/deploy-assets, production restart of `vcontrolhub-next.service` and `vcontrolhub-ssh-ws.service`, `/api/status` healthy 3/3, `./deploy/smoke-test.sh whrkhldsb.qzz.io vcontrolhub` 25/25, browser login verified the visible global search dialog, notification dropdown, and `/downloads` page without alerts or horizontal overflow.
    - [x] 2026-06-08 — Follow-up comprehensive functional availability sweep focused on small closable UX/function gaps rather than opening a new large architecture lane. Preflight showed git clean, HEAD == origin/main `63dbb5c`, services active, `/api/status` healthy, and recent logs clean. Static scan and README/backlog review identified several remaining candidates (durable jobs, request-bound backup/SFTP, true deployment rollback, AI hosted-tool authorization, share/archive/media stream gaps, focus management/mobile navigation). Closed the smallest high-confidence item from the README accessibility boundary: global search now records the triggering element and restores focus to it when Escape or backdrop click closes the dialog. Regression added for visible-opener focus restore; targeted search/sidebar tests passed 15/15.
    - [x] 2026-06-08 — Continued the functional availability/accessibility sweep by closing and deploying the `/files` create-folder form label gap. The expanded form now renders visible labels for the target storage node and folder-name field instead of relying on hidden text plus placeholder-only guidance, and the regression uses label queries for the submit path. Verification: `git diff --check`, targeted Files form/list/page regressions 27/27, full `npm run verify` including full Vitest/Next build/runtime build/deploy-assets, `.next`/`dist` ownership repair, restart of `vcontrolhub-next.service` and `vcontrolhub-ssh-ws.service`, `/api/status` healthy 3/3, smoke 25/25, and recent service logs clean.
  - [ ] QA batch 2 — VPS/server management and SSH terminal. Add/edit/delete-safe paths where practical, connection/status feedback, Direct Gateway eligibility/copy, browser terminal idle stability, service health badges, and user-facing recovery guidance.
  - [x] 2026-06-01 — Started QA batch 2 with production browser login as a temporary operator, opened `/servers`, expanded a real VPS card, inspected Direct Gateway/storage/status details, toggled light theme, and opened a real browser SSH terminal session. Visual QA found the SSH terminal opened successfully and connected, but the modal had no `role="dialog"`/`aria-modal`/labelled title semantics and its light-theme header/control contrast was weaker than the surrounding shell. Fixed `SshTerminalModal` to expose a labelled modal dialog with described host, announced connection status, command-panel `aria-expanded`, an explicitly labelled close button, and light/dark readable header/control styling. Verification rerun/closeout: SSH terminal modal/url + server controls regressions 7/7, `git diff --check`, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart with `.next` ownership repair, smoke 19/19, `/servers` 307, `/api/auth/ws-token` 401, `/login` 200, `/api/status` 200, recent service logs clean, production browser DOM proof from the originating QA showed `role=dialog`, `aria-modal=true`, title/description/status/close label, and real SSH prompt visible. Temporary `qa_cron_*` cleanup verified 0 remaining. Pending: major-round bundled commit/push after state/roadmap closeout.
  - [x] 2026-06-01 — Continued QA batch 2 Direct Gateway recovery-guidance browser QA. Production browser QA expanded a real VPS card and found the Direct Gateway control was readable but under-explained for recovery/destructive switch-back use; added accessible gateway-control labelling/status, public URL probe link, light-theme readable recovery guidance for eligibility/listening-port/firewall checks, explicit truthful failure copy, and focus-visible affordances for the SSH/gateway controls. Verification: direct-gateway/server/SSH neighboring regressions 12/12, git diff check, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart with `.next` ownership repair, smoke 19/19, `/servers` 307, `/api/auth/ws-token` 401, `/login` 200, `/api/status` 200, recent service logs clean, browser light-theme visual proof showed guidance/link/button readable. Temporary `qa_cron_*` cleanup verified 0 remaining. Commit: 1d3b1da.
  - [x] 2026-06-01 — Dirty-worktree closeout for QA batch 3 Files/file-manager accessibility work left by a prior run. The Files batch toolbar now has a named `role="region"` with a selected-count description, partial batch delete/move failures are announced through a labelled `role="alert"`, and the batch move target field is labelled by role/name instead of placeholder-only text. Verification: `git diff --check`, `npx vitest run src/app/files/__tests__/file-list-client.test.tsx` 12/12, neighboring Files/storage regressions 14/14, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart with `.next` ownership repair, smoke 19/19, `/files` 307, `/api/files/list` 401, `/api/storage/sftp` 401, `/login` 200, `/api/status` 200, recent service logs clean.
  - [x] 2026-06-01 — Continued QA batch 2 server-management status/readability polish. Server cards now expose the enable/disable badge as an accessible `role="status"`, bind the details toggle to a named per-VPS details region, add operator guidance that the badge reflects VControlHub management state rather than proof of SSH/file/direct-gateway health, and improve the expanded server detail cards for light-theme readability. Verification: `git diff --check`, server page + Direct Gateway neighboring regressions 11/11, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart with `.next` ownership repair, smoke 19/19, `/servers` 307, `/api/auth/ws-token` 401, `/login` 200, `/api/status` 200, recent service logs clean. Pending: major-round bundled commit/push after state/roadmap closeout.
  - [x] 2026-06-01 — Continued QA batch 2 destructive VPS delete/edit-path safety. The server card delete flow now uses an accessible in-page `alertdialog`, explains that deletion removes the VPS list row and synchronizes associated storage/direct-gateway cleanup, requires typing the current VPS name before the server action calls `deleteServerProfile`, and keeps edit/toggle/delete controls readable in light theme with alert/status semantics. Verification: `git diff --check`, server action/page/Direct Gateway neighboring regressions 16/16, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart with `.next` ownership repair, smoke 19/19, `/servers` 307, `/api/auth/ws-token` 401, `/login` 200, `/api/status` 200, recent service logs clean. Pending: major-round bundled commit/push after state/roadmap closeout.
  - [x] 2026-06-01 — Continued QA batch 2 SSH terminal idle-stability production QA. Production browser QA logged in as a temporary operator, opened `/servers`, launched a real SSH terminal for VPS `45.207.216.45`, confirmed the Debian banner/root prompt and connected status remained visible after the browser-tool timeout window with no disconnected/error text, and verified `vcontrolhub-ssh-ws.service` had no recent log entries while production services and `/api/status` were healthy. Temporary `qa_cron_*` cleanup verified 0 remaining. During closeout, recent Next logs revealed an unrelated `/files/preview` runtime crash (`DOMPurify.sanitize is not a function`); patched text/markdown preview sanitizer initialization so production bundles instantiate DOMPurify with `window` when needed instead of crashing. Verification so far: markdown preview sanitizer regression 2/2 and `npm run typecheck` passed. Pending: lint/build/runtime build, restart/smoke/probes/log checks, backlog/state closeout, and major-round commit/push after the Files preview fix is fully deployed.
  - [x] 2026-06-01 — Continued QA batch 3 Files/Media/Traffic real-path closeout after prior dirty worktree handoff. Production browser/API QA verified `/api/files/list` returns grouped storage-node roots including empty `本机默认存储（LOCAL）`, `/api/media` returns real media items, and `/api/traffic/summary` returns explainable storage traffic sources (`当前服务器网卡`, `绑定服务器：…`) rather than blank/unknown source labels. Fixed Files tree grouping so configured storage nodes stay visible across SSR and SPA refresh even when empty, made preview/media classification infer media from file extension when MIME is blank/generic, repaired DOMPurify client-bundle sanitizer initialization for text/markdown previews, added Media Library preview/download/source actions, and returned richer Traffic Center source labels/details. Verification: browser login with temporary `qa_cron_*` admin; Files/Media/Traffic API + page DOM evidence; temporary QA cleanup verified 0 remaining; targeted regressions 14/14; `npm run typecheck`; `npm run lint` (existing warnings only); `npm run build`; `npm run build:runtime`; production restart with `.next` ownership repair; smoke 19/19; `/files` `/media` `/traffic` 307 unauth, related APIs 401 unauth, `/api/status` 200; post-deploy logged-in API and page proof; recent service logs clean. Pending commit/push in this closeout tick.
  - [x] 2026-06-02 — QA batch 3 SFTP Direct Gateway + unified Files SPA closeout. Change Contract: SFTP Browser Direct Gateway mode could show ON while download links were unusable when `publicUrl` already included the proxy port (`host:port:port`) and raw path/token concatenation was used; additionally, remote SFTP browsing was not fully tied into the unified Files SPA/preview path. Code now centralizes direct proxy URL building, embeds the SFTP browser in `FilesBrowserSpa`, refreshes `/api/files/list` when the remote SFTP directory changes, and renders real `/files/preview` links for previewable remote SFTP files. Verification: targeted Files/SFTP regressions 27/27, typecheck, lint (existing warnings only), build, runtime build, git diff check, production restart with `.next` ownership repair, smoke 19/19, logged-in browser Files page verified SFTP selection updates the unified tree/list and renders preview links, text preview opens via `/files/preview`, recent service logs clean.
  - [x] 2026-06-03 — Dirty-worktree closeout for QA batch 3 recycle-bin permanent delete truthfulness. Found a previously prepared storage slice where `permanentDeleteFileEntryAction` should still remove a recycled SFTP row when the remote backing file is already gone out-of-band; otherwise operators can get stuck with undeletable recycle-bin entries even though the real file is already absent. Added a shared backing-object delete helper plus a targeted missing-object classifier so ordinary delete-to-recycle-bin still fails closed, while permanent delete tolerates ENOENT/“No such file” and continues DB cleanup. Verification: `npx vitest run src/app/storage/__tests__/actions.test.ts` 10/10, neighboring `src/app/files/__tests__/file-list-client.test.tsx` included in a combined 23/23 run, `git diff --check`, `npm run typecheck`, `npm run lint` (known 5 warnings only after removing stray temporary helper `.hermes/summarize-sftp-canary.cjs`), `npm run build`, `npm run build:runtime`, `.next` ownership repair, restart of `vcontrolhub-next.service` + `vcontrolhub-ssh-ws.service`, smoke 24/24, `/api/status` healthy 3/3, recent logs clean. Commit: b67b74eec3a0792f504a97f2898c640dda1ef7ec.
- [x] 2026-06-04 — QA batch 3 LOCAL storage upload/download placeholder closeout. Production canary found that default LOCAL storage `node_local_default` still persisted `basePath=/var/lib/${APP_SLUG:-vcontrolhub}/storage`, and authenticated POST `/api/storage/local` failed HTTP 500 with ENOENT while trying to create the literal `/var/lib/${APP_SLUG:-vcontrolhub}` directory. Runtime storage path resolution now expands `APP_SLUG` placeholders before resolving local base paths in both shared path helpers and the `/api/storage/local` route. Verification: pre-fix failing upload captured, targeted storage/list regressions 23/23 plus path-utils 9/9, `git diff --check`, typecheck, lint, build, runtime build, ownership repair, restart of `vcontrolhub-next.service` + `vcontrolhub-ssh-ws.service`, smoke 24/24, post-fix authenticated upload/list/download proved a 90-byte file under `/var/lib/vcontrolhub/storage/...`, Prisma FileEntry/file stat proof matched, recent logs had only the expected pre-fix ENOENT and no new post-fix storage errors, cleanup removed the canary file/row and verified qa_cron_* users 0 / QuickService rows [] / no qs-* containers.
  - [ ] Continue QA batch 2 with SSH terminal idle stability; then rotate to QA batch 3 deeper Files/storage/SFTP upload/download/direct-access user-path testing.
  - [ ] QA batch 3 — Files/storage/SFTP/upload/download/direct access. Test empty states, SFTP sync partial-failure warnings, upload limits, rename/move/delete dialogs, preview/download permissions, share links, and signed/direct-access behavior with visual verification.
  - [ ] QA batch 4 — Downloads, Quick Services, Commands, Requests, Deployments, Scheduled Tasks, Templates, and Operation Tasks. Verify long-running task status is truthful, cancellation/errors are visible, target selection dedupes, worker diagnostics are understandable, and no workflow looks successful without the real side effect.
  - [x] 2026-06-03 — Verified the backup/restore canary in production without product code changes. The Backups page and backup routes continued to gate restore behind explicit backup:restore plus RESTORE confirmation, the backup service still resolves writable artifacts under /var/backups/vcontrolhub/backups/... via BACKUP_DIR/APP_SLUG, and the targeted backup Vitest plus full quality gates passed before restart/smoke. Verification: `npx vitest run src/lib/backup/__tests__/service.test.ts src/app/backups/__tests__/page.test.ts src/app/api/backups/__tests__/route.test.ts src/app/api/backups/[id]/restore/__tests__/route.test.ts`, `npm run typecheck`, `npm run lint`, `npm run build`, `npm run build:runtime`, `systemctl restart vcontrolhub-next.service vcontrolhub-ssh-ws.service`, `./deploy/smoke-test.sh whrkhldsb.qzz.io vcontrolhub` 24/24, recent logs clean, no temp QA users/artifacts left behind.
  - [x] 2026-06-04 — Verified the Settings runtime canary in production without product code changes. Runtime `operationTaskListLimit` was visible in the Settings UI, missing-CSRF PATCH failed closed with 403 and no persistence, CSRF-protected PATCH changed the value to 123, `/operation-tasks` and `/api/operation-tasks` continued to hydrate through the persisted runtime setting, then the value was rolled back to 100. Verification: production browser login with temporary `qa_cron_settings_canary` admin, authenticated `/api/settings` and `/api/operation-tasks` proof, production Prisma setting row/qa-user cleanup proof, targeted runtime/settings/Operation Tasks regressions 21/21, `./deploy/smoke-test.sh whrkhldsb.qzz.io vcontrolhub` 24/24, services active, recent logs clean, no product code change or restart required.
- [x] 2026-06-04 — Verified the QA batch 2 Servers/SSH/Gateway canary in production without product code changes. Production browser login with temporary `qa_cron_servers_canary` opened `/servers`, confirmed 3 enabled VPS / 3 bound storage nodes, expanded `45.207.216.45`, verified connection/storage/direct-gateway guidance, and launched a real SSH terminal that reached `已连接` with accessible dialog semantics. Prisma inspection confirmed the three enabled Server rows and storage bindings; targeted Servers/SSH regressions passed 4/4; smoke passed 24/24; services stayed active; logs had no SSH/WebSocket failures. Cleanup deleted the QA user, removed temporary `/tmp` credentials, and also cleaned a stale AList Quick Services row/container left from a prior canary so final `quickServices=[]`, `qa_cron_*` count 0, and `docker ps` showed no `qs-*` containers.
- [x] 2026-06-04 — Verified and fixed the Monitoring/Health/System Health canary. Production browser QA proved `/health` rendered Health Center self-checks (16 checks, 15 healthy, 1 warning, 0 critical), `/api/system-health` and `/api/health` returned authenticated observability payloads, and `/monitoring` rendered live host resource/process data. Found Monitoring metric truthfulness defects: disk copy displayed total/used while saying used, and the Top processes MEM% column showed impossible raw values such as `3128084M`. Fixed `/api/monitoring/stats` to report disk as used/total and process RSS as bounded memory percentages after robust `/proc/[pid]/stat` parsing. Verification: monitoring/health/system-health route regressions 10/10, `git diff --check`, `npm run typecheck`, `npm run lint`, `npm run build`, `npm run build:runtime`, ownership repair, restart of `vcontrolhub-next.service` + `vcontrolhub-ssh-ws.service`, smoke 24/24, authenticated browser/API proof after deploy, recent logs clean, QA user cleanup verified 0 remaining.
- [x] 2026-06-04 — Verified and fixed the QA batch 5 API Tokens + image-bed integration canary. Production login and CSRF-backed `/api/api-tokens` creation worked, and Bearer `/api/images/list` worked, but the real image upload side effect returned `500 {"error":"上传失败"}` because `vcontrolhub-next.service` runs under `ProtectSystem=strict` and its `ReadWritePaths` omitted the app image upload/runtime data directories. Updated the Next.js systemd unit template to grant `{{APP_DIR}}/uploads`, `storage`, `downloads`, `backups`, and `logs`; applied the matching live override for the hosted instance including `/opt/whrkhldsb/uploads`; repaired ownership; restarted services; then reran the production API-token image workflow successfully: upload 201, list/file proof, file endpoint 200 image/png 68 bytes, DELETE 200 cleanup. Verification: image upload/publish/file regressions 13/13, `git diff --check`, `npm run typecheck`, `npm run lint`, `npm run build`, `npm run build:runtime`, restart of `vcontrolhub-next.service` + `vcontrolhub-ssh-ws.service`, smoke 24/24, recent post-fix logs clean of new image-bed/EACCES failures, and temporary QA user/token/files cleanup verified 0 remaining.

## Fresh audit findings — 2026-05-30 manual review

- [x] 2026-06-07 — Verified the QA batch 4 Downloads negative-boundary canary in production without product code changes. Authenticated temporary admin access opened `/downloads` (200) and `/api/downloads` (200 with tasks/globalStat), selected an enabled VPS with bound SFTP storage, and exercised safe fail-closed boundaries without starting a real download: missing-CSRF PATCH returned 403, mixed HTTP + magnet `isBatch/batchUrls` POST returned 400 before task creation, and missing-task refresh PATCH returned 404. Prisma proof showed canaryTaskCount 0 for the canary target/url, cleanup removed the temporary QA user (`qa_cron_*` remaining 0), smoke passed 25/25, `/api/status` was healthy, services stayed active/enabled, recent next/ssh-ws logs were clean, and git stayed clean with HEAD == origin/main d4e9970.
- [x] 2026-06-07 — Closed README P2 storage-node metadata authorization boundary. `/api/storage/nodes` now preserves full node metadata only for `storage:manage-node` sessions, while ordinary `storage:read` users receive only nodes covered by readable `UserStorageAccess` grants, preventing unauthorized `basePath`/server-binding disclosure. Verification: route regression 4/4, typecheck, lint `--max-warnings=0`, full `npm run verify` passed (207 files / 940 tests plus Next build/runtime/deploy-assets); production restart, stable `/api/status` 200, smoke 25/25, recent logs clean; production canary with temporary viewer + one SFTP read grant returned only the granted node and cleanup verified `qa_cron_*` users 0.

- [x] 2026-06-07 — Closed README P0 remote file proxy scope/header/CORS boundary. `/api/servers/[id]/file-proxy` now requires a bound SFTP storage node before starting the temporary target-host Python proxy, generates `SERVE_DIR` from that storage node `basePath` instead of `/`, enforces target-side realpath containment, accepts `Authorization: Bearer` / `X-VControlHub-Proxy-Token` before legacy query tokens, and restricts CORS to the Hub origin with `Referrer-Policy: no-referrer` and `X-Content-Type-Options: nosniff`. Verification: targeted file-proxy route tests 5/5 passed, typecheck passed, lint 0 warnings, full `npm run verify` passed. Deploy: runtime build passed, production services restarted/reloaded, `/api/status` healthy, smoke 25/25 passed, recent service logs clean.
- [x] 2026-05-30 — Add server-side session/permission guards to client-heavy monitoring, traffic, docker, preferences, api-docs, and image-bed pages so unauthenticated/under-authorized users are redirected or shown permission denial before the browser mounts and starts API polling/upload/list calls. Existing client behavior moved into explicit `*-page-client` components with page-level wrappers enforcing `health:read`, `server:read`, `docker:manage`, `image:read`, or authenticated-session policy. Verification: page/client regressions 15/15, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/monitoring` `/traffic` `/docker` `/preferences` `/api-docs` `/image-bed` probes returned 307 and related APIs returned 401/200 without 500, recent logs clean.
- [x] 2026-05-30 — Fix global search/navigation completeness: global search now derives its page catalog from the shared sidebar navigation items so main/system nav routes stay covered, routes password/2FA actions to concrete `/settings#password` and `/settings#2fa` anchors instead of dispatching dead modal events, and the Settings page exposes matching scroll anchors. Verification: global-search/settings regressions 8/8, typecheck, lint, build, runtime build, production restart, smoke 19/19, `/settings` 307, `/api/settings` 401, `/api/status` 200, recent logs clean. Commit: 6740ac6.
- [x] 2026-05-30 — Make Preferences real instead of no-op saved settings: shared preference normalization now filters stale `compactMode`/`sidebarCollapsed`, rejects unknown default pages/widgets, reuses shared refresh interval bounds/defaults, consumes `defaultPage` in login redirect when no explicit `next` is present, and consumes `dashboardWidgets` on the dashboard so operators can hide/show dashboard sections from saved/local preferences. Verification: preference API/page/dashboard/login regressions 13/13, typecheck, lint (existing warnings only), build, runtime build, production restart, smoke 19/19, `/preferences` 307, `/` 307, `/login` 200, `/api/preferences` 401, `/api/status` 200, recent logs clean.
- [x] 2026-05-31 — Improve manual SFTP scan availability by bounding each remote directory scan with a configurable timeout (`SFTP_SYNC_DIRECTORY_TIMEOUT_MS`, default 60s), returning 504 for complete scan failure and 207 for partial success with errors, and showing an amber “同步未完全完成” alert in the Files SFTP browser instead of a green success banner when remote scanning fails. Verification: SFTP sync service/API/browser regressions 12/12, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart, smoke 19/19, `/files` 307, `/api/storage/sftp-sync` 401, `/api/status` 200, recent logs clean. Commit: 5ae1668.
- [x] 2026-05-30 — Split storage/file permissions into per-entry read/write/delete capabilities in the Files UI and API payloads. `/files` SSR data and `/api/files/list` SPA refreshes now serialize capabilities from explicit `UserStorageAccess` grants, preview/download controls require read capability, rename/move/edit require write capability, delete/batch delete require delete capability, and batch selection only includes entries the caller can actually mutate. Verification: storage access + Files page/API/client regressions 26/26, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart, smoke 19/19, `/files` 307, `/api/files/list` 401, `/api/storage/sftp` 401, `/api/status` 200, recent logs clean. Commit: 5bf1693.
- [x] 2026-05-30 — Improve file manager ergonomics: file-size sorting now uses API/server-provided numeric bytes instead of localized size labels, selection state clears when operators change directory/search scope, sort icon buttons have reader-friendly labels, and table checkboxes are visible without hover-only discovery. Verification: file-list/files-page regressions 21/21, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart, smoke 19/19, `/files` 307, `/api/files/list` 401, `/api/storage/sftp` 401, `/api/status` 200, recent logs clean. Commit: a3c6afc.
- [x] 2026-05-30 — Replace raw browser `confirm`/`prompt` dialogs in shipped destructive/edit flows with accessible in-app dialogs and explicit success/error states. Backup restore now uses a typed `RESTORE` modal, announcement/snippet deletion use modal confirmation plus toast/error feedback, and AI conversation rename uses an in-app rename dialog instead of `prompt`; a focused source scan shows no remaining `confirm(`/`prompt(` calls under `src/app`. Verification: destructive-dialog regressions 12/12, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart, smoke 19/19, `/backups` `/announcements` `/snippets` `/ai` 307, related APIs 401, `/api/status` 200, recent logs clean. Commit: de07c57.
- [x] 2026-05-30 — Add dialog/switch/button accessibility polish for global search and preference toggles. Global search now exposes a labelled modal dialog, combobox, listbox/options, active descendant, and selected state for keyboard/screen-reader users; Preferences dashboard/notification toggles now render as labelled `role="switch"` controls with `aria-checked`, focus-visible rings, and an announced save status. Verification: global-search/preferences regressions 10/10, full Vitest 176 files / 719 tests, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart, smoke 19/19, `/preferences` 307, `/api/preferences` 401, `/api/status` 200, recent logs clean. Commit: e5deb40.
- [x] 2026-05-30 — Reduce storage grant quota checks from large in-memory `FileEntry.findMany(select size)` scans to a database-side `_sum(size)` aggregate, so write authorization enforces per-grant quota without loading every matching file row into the API worker. Verification: access-control regression proves aggregate query shape and no file-entry list scan; neighboring storage route/sync tests 28/28; full Vitest 176 files / 721 tests; typecheck; lint (existing client-effect warnings only); build; runtime build; production restart; smoke 19/19; `/files` 307, `/api/files/list` 401, `/api/storage/local` 401, `/api/status` 200; recent logs clean. Commit: 25950a9.
- [x] 2026-05-30 — Harden management-configured Direct Access URLs for storage nodes so operators cannot persist or redirect through localhost, private, link-local, loopback, multicast/special IPs, credentialed URLs, or non-HTTP(S) schemes. Storage-node create/edit schemas now validate public base URLs before persistence, service writes normalize safe URLs (including bracketed IPv6), and the signed direct-access route fails closed instead of redirecting if an older stored URL is unsafe. Verification: storage schema/service/direct-access route regressions 30/30, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart, smoke 19/19, `/storage` `/files` 307, `/api/storage/direct-access` `/api/storage/sftp-sync` `/api/storage/nodes` 401, `/api/status` 200, recent logs clean. Commit: 3ee3667.
- [x] 2026-05-31 — Bound SyncJob list hydration for growing task/sync surfaces: `listSyncJobs()` now caps newest-first rows at 200 so future Sync UI/Task Center callers cannot accidentally hydrate unbounded sync-job history. Verification: sync service regression proves `take: 200`, targeted Vitest 9/9, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart, smoke 19/19, `/files` 307, `/api/storage/sftp-sync` 401, `/api/operation-tasks` 401, `/api/status` 200, recent logs clean.
- [x] 2026-05-31 — Bound share-link list hydration for growing public-link history: `listShareLinks()` now caps newest-first rows at 200 while preserving storage-node/creator metadata for the Shares/API surfaces. Verification: share-link service/API regressions 7/7, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart, smoke 19/19, `/shares` 307, `/api/share-links` 401, `/api/status` 200, recent logs clean.

- [x] 2026-05-31 — Bound backup history list hydration for growing backup/audit surfaces: `listBackupRecords()` now caps newest-first rows at 200 while preserving creator metadata for the Backups page/API. Verification: backup service/API/restore regressions 21/21, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart, smoke 19/19, `/backups` 307, `/api/backups` 401, `/api/status` 200, recent logs clean. Commit: 071eee3.
- [x] 2026-05-31 — Bound API-token history hydration for growing integration surfaces: `listApiTokens()` now caps newest-first rows at 200, and the API Token page explicitly requests the bounded window before passing safe token metadata to the client. Verification: API-token service/route/client regressions 13/13, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart, smoke 19/19, `/api-tokens` 307, `/api/api-tokens` 401, `/api/status` 200, recent logs clean. Commit: 3713cd9.
- [x] 2026-05-31 — Bound deployment/download target-server hydration for growing VPS fleets: Deployments and Downloads pages now cap enabled-server selectors at 200 while preserving existing ordering and selected safe fields, preventing large server inventories from making form bootstrap queries unbounded. Verification: deployment/download page regressions 3/3, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart, smoke 19/19, `/deployments` 307, `/downloads` 307, `/api/deployments` 401, `/api/downloads` 401, `/api/status` 200, recent logs clean. Commit: e1439b5.
- [x] 2026-05-31 — Bound scheduled-task and command-template list hydration for growing automation surfaces: scheduled task lists, command template lists, and deployment launch template lists now cap newest/built-in-first rows at 200 so `/scheduled-tasks`, `/templates`, and deployment launch APIs cannot hydrate unbounded automation history/catalog rows. Verification: scheduled-task/command-template/deployment service regressions 13/13, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart with `.next` ownership repair, smoke 19/19, `/scheduled-tasks` 307, `/templates` 307, `/api/scheduled-tasks` 401, `/api/command-templates` 401, `/api/deployments` 401, `/api/status` 200, recent logs clean. Commit: 2c4ab56.

- [x] 2026-05-31 — Productize operator-tunable runtime settings first slice instead of leaving stability/UX knobs hidden in `.env`: added a non-sensitive runtime settings service plus admin Settings UI/API fields for command execution timeout, output limit, stale-running recovery window, command heartbeat, command maintenance scan interval, and SFTP per-directory sync timeout. Command execution, maintenance worker scheduling, and SFTP sync now read persisted settings with env/default fallback, with range validation and operator copy for immediate-vs-restart effects. Verification: runtime/settings/command/SFTP regressions 36/36, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart, smoke 19/19, `/settings` 307, `/api/settings` 401, `/api/storage/sftp-sync` 401, `/api/commands` 401, `/api/status` 200, recent logs clean.
- [x] 2026-05-31 — Continue operator-tunable runtime settings coverage for SSH terminal connection stability: Settings now exposes bounded SSH WebSocket heartbeat interval, SSH keepalive interval, and SSH keepalive tolerance controls; `/api/settings` validates/normalizes them; `ssh-ws-proxy` reads persisted values with env/default fallback for the heartbeat timer and per-connection ssh2 keepalives. Verification: runtime/settings/API/SSH-WS regressions 21/21, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart with artifact ownership repair, smoke 19/19, `/settings` 307, `/api/settings` 401, `/api/auth/ws-token` 401, `/api/status` 200, recent logs clean. Commit: 9fe5fff.
- [x] 2026-05-31 — Continue operator-tunable runtime settings coverage for bounded task-center hydration: Settings now exposes a bounded `runtime.operationTaskListLimit` control (20–500, default/env fallback 100), `/api/settings` validates/normalizes it, and both `/operation-tasks` SSR plus `/api/operation-tasks` cap command/scheduled/download/sync/backup/deployment hydration through the shared runtime setting instead of a hard-coded API/page limit. Verification: runtime settings, Operation Tasks service, Settings UI/API regressions 21/21; typecheck; lint (existing client-effect warnings only); build; runtime build; production restart with `.next` ownership repair; smoke 19/19; `/settings` 307, `/operation-tasks` 307, `/api/settings` 401, `/api/operation-tasks` 401, `/api/status` 200; recent logs clean.
- [x] 2026-05-31 — Continue refresh/polling preference coverage for notification unread fallback polling: when WebSocket realtime notifications are unavailable, the Notification Bell now uses the saved global refresh preference instead of a hard-coded 30s interval, disables fallback polling when the preference is manual, reacts to preference-change events, and labels fallback state as realtime/manual/polling cadence in the dropdown. Verification: notification bell regressions 6/6, health/monitoring neighboring refresh regressions 13/13, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart, smoke 19/19, `/notifications` 307, `/api/notifications` 401, `/api/preferences` 401, `/api/status` 200, recent logs clean. Commit: 0b7e5d6.
- [x] 2026-05-31 — Continue operator-tunable runtime settings coverage for other bounded-list limits where user-facing tuning is appropriate: AI provider and AI conversation list hydration now read bounded admin Settings controls (`runtime.aiProviderListLimit`, `runtime.aiConversationListLimit`) instead of fixed service constants, preserving safe env/default fallbacks and range validation. Verification: AI service/runtime/settings API/Settings UI regressions 22/22, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart, smoke 19/19, `/settings` 307, `/ai` 307, `/api/settings` 401, `/api/ai/providers` 401, `/api/ai/conversations` 401, `/api/status` 200, recent logs clean. Commit: f651ae4.
- [x] 2026-05-31 — Improve Settings light-theme runtime-tuning usability discovered during the visual QA gate: runtime parameters now render as a compact two-column labelled grid with explicit light-mode label/input contrast, and the global compatibility layer maps legacy `text-white/50`/`text-white/40` utilities to readable muted text in light theme. Verification: Settings component/CSS regressions and monitoring CSS regression 11/11, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart, smoke 19/19, `/settings` 307, `/api/settings` 401, `/api/status` 200, browser login + light-theme visual verification confirmed labels are visible and grouped compactly. Commit: 903a4ad.
- [x] 2026-05-31 — Continue Settings visual-QA polish: SMTP connection fields now clearly reflect the disabled state when SMTP is off, with disabled inputs, helper text, readable light-theme styling, and accessible labelled switch controls so operators do not think inactive SMTP parameters are currently applied/editable. Verification: SettingsClient regression 9/9, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart, smoke 19/19, `/settings` 307, `/api/settings` 401, `/api/status` 200, browser login + light-theme visual verification confirmed disabled SMTP fields and runtime labels are readable. Commit: da0c328.

- [x] 2026-05-31 — Continue completion-gate visual QA polish for dashboard/localization: dashboard summary/recent-activity sections now consume the active language context instead of keeping hard-coded Chinese labels, sign-out text uses the same localized text helper, and server-rendered dashboard data is mapped to JSON-safe props before crossing into client components to avoid Date/complex-object serialization regressions. Verification: dashboard-localized/dashboard-analytics/mobile-nav regressions 10/10, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart with .next ownership repair, smoke 19/19, /login 200, / 307, /settings 307, /api/dashboard/analytics 401, /api/status 200, recent logs clean.
- [x] 2026-05-31 — Continue completion-gate API-token operator-flow QA: API Token revocation now keeps the destructive confirmation dialog open and shows the backend error inline when the revoke API fails, so the UI no longer closes the modal or implies success while the token remains active. Verification: API-token client/API/service regressions 14/14, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart with .next ownership repair, smoke 19/19, `/api-tokens` 307, `/api/api-tokens` 401, `/login` 200, `/api/status` 200, recent logs clean. Commit: 1cf78e1.
- [x] 2026-05-31 — Continue completion-gate AI provider setup availability QA: AI provider creation/probe now preserves the service-layer default Base URL when operators leave Base URL blank, and create/edit paths normalize model lists consistently so duplicate/blank model IDs are not persisted into provider configuration or shown as misleading success. Verification: AI provider panel/API/service regressions 14/14, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart, smoke 19/19, `/ai` 307, `/api/ai/providers` 401, `/api/ai/models/probe` 401, `/settings` 307, `/api/status` 200, recent logs clean. Commit: 7e423bc.
- [x] 2026-05-31 — Continue completion-gate image-bed publish-from-storage QA: publishing an existing LOCAL storage image to the image bed now authorizes the exact source storage path with `assertStorageAccess` before reading the file, and cleans up the just-written public image-bed file if `ImageUpload.create` fails so the API cannot report failure while leaving an orphan public object. Verification: publish/upload/delete image route regressions 13/13, typecheck, lint (existing client-effect warnings only), build, runtime build, production restart with `.next` ownership repair, smoke 19/19, `/image-bed` 307, `/api/images/publish-from-storage` 401, `/api/images/list` 401, `/api/images/stats` 401, `/api/status` 200, recent logs clean. Commit: fad0d88.


## Process evaluation — 2026-05-31

- [x] 2026-05-31 — Evaluated today’s backlog remediation results and task/process quality after the backlog cron was stopped. Confirmed the repository is clean, `main` matches `origin/main`, production services (`vcontrolhub-next`, `vcontrolhub-ssh-ws`, `caddy`) are active, `/api/status` is healthy, recent service logs show no error/warn matches, and watchdog has been silent after the earlier dirty-worktree closeout.
- Result quality: today delivered real improvements in runtime configurability, bounded-list performance, notification/refresh behavior, Settings light-theme/runtime UX, SMTP disabled-state clarity, and dashboard localization/client-boundary safety, each with targeted tests plus typecheck/lint/build/runtime-build/restart/smoke/probes/log checks recorded in state/backlog.
- Gap found: the stopped backlog cron `b8833e83bb1f` was still described as the current remediation job in repo handoff files, and the completion QA gate still needs to be enforced as real browser/user-path QA before any new ordinary backlog loop.
- Improvement for future recreated task: broaden every cycle beyond architecture to functional completeness, code quality, performance, reliability, UI/UX/accessibility, permissions/security, tests, deployment/runtime health, and real workflow side effects. Static 307/401/no-500 probes are required but insufficient.
- Next mode activated: recreated “清单检查修复” as `9e36e64a75ae` with an explicit full-product tester prompt. First runs should prioritize completion QA batches and real user workflow verification across VPS/server management, Files/SFTP/upload/download/direct access, Downloads, Quick Services, Commands/Requests/Deployments/Scheduled Tasks/Templates/Operation Tasks, Backups/restore, Health/Monitoring, Settings, Tickets/Announcements, API Tokens, AI providers, image bed, API docs, and audit/security-adjacent flows.

## Process improvement — 2026-06-01

- [x] 2026-06-01 — Incorporated user feedback into the autonomous remediation process: reduce GitHub commit noise by bundling related work into major-round closeout commits instead of pushing every tiny successful tick; each major round must finish with a comprehensive check and update GitHub-visible future goals/roadmap docs when priorities shift, including the GitHub homepage `README.md` roadmap plus `docs/plans/next-ops-features-plan.md` and `docs/plans/2026-05-28-usability-first-modernization.md`; Settings should become a richer customization/control surface for safe runtime, UI, notification, integration, and feature behavior rather than a narrow admin form.
- Next execution rule: the active full-product QA remediation loop should treat Settings customization as a near-term product-completeness lane, but still rotate through real user-path QA batches and avoid endless Settings-only polish.

## Process improvement — 2026-06-01 precision control loop

- [x] 2026-06-01 — Upgraded the two remaining cron jobs into a closed-loop QA control system. The main remediation job must now consult `.hermes/remediation-state.json` `qaControlSystem.nextQaPathQueue`, honor any `dailyAuditorDirective`, and bind each run to a concrete QA path plus evidence profile. The daily review job must score precision/target clarity/side effects/rotation/noise/safety, inspect a 7-day trend window, and write a structured directive when the main loop drifts.
- [x] 2026-06-02 — Expanded that control loop into a full-function canary evidence QA system. The active main job `9e36e64a75ae` is now named `VControlHub full-product QA remediation loop — canary evidence QA` and must start every run with a Change Contract plus Evidence Matrix, handle dirty worktrees as closeout-only work, and prove selected user journeys with browser/API/DB/filesystem-or-SFTP/process/log/regression/production evidence instead of relying on route liveness probes.
- [x] 2026-06-02 — Re-enabled daily precision auditor `d42e2078d544` as `VControlHub daily QA strategy review — canary precision auditor` on schedule `0 18 * * *`. It is explicitly audit-only by default: no product code changes, no deployment, no new cron; it scores main-loop quality and writes `qaControlSystem.dailyAuditorDirective` when coverage or evidence quality drifts.
- [x] 2026-06-02 — First canary precision audit found the main loop was truthfully preserving a blocked Commands/Operation Tasks dirty closeout, but had started to burn too many repeated retries on the same approval-gated restart. The auditor now requires a closeout-only directive for that slice: one narrow activation retry per tick, no broadened QA-path rotation while the dirty diff remains unchanged, and no repeated full build/typecheck reruns unless code changes or `.next` ownership drifts again.
- [x] 2026-06-02 — Added ten durable canary lanes to `.hermes/remediation-state.json`: login/shell/preferences, servers/SSH/gateway, files/storage/SFTP, downloads lifecycle, Quick Services Docker side effects, commands/deployments/tasks, backups/restore, settings runtime effects, monitoring/health/status, and integrations/media/security. High-priority canaries should receive real evidence within a 7-day window unless a higher-priority blocker/directive is recorded.
- [x] 2026-06-03 — Commands/Operation Tasks dirty closeout completed successfully. The known_hosts fix is now active in production: after repairing `.next/server` ownership and restarting `vcontrolhub-next.service` + `vcontrolhub-ssh-ws.service`, `/api/status` returned healthy 3/3, `./deploy/smoke-test.sh whrkhldsb.qzz.io vcontrolhub` passed 24/24, and a fresh authenticated post-fix command canary completed with `exitCode 0`, stdout `vcontrolhub-knownhosts-postfixLinux`, and `stderr: null` after adding `LogLevel=ERROR` to both key and password SSH command paths. Temporary `qa_cron_*` users were cleaned to zero and recent production logs stayed clean of known_hosts/EACCES warnings.
- [x] 2026-06-03 — Reconciled a stale dirty-worktree preflight against the live repo. The previously reported uncommitted Files/Preferences/SFTP closeout was already committed and pushed as `22d9bab` (`fix: harden files selection and async refresh flows`), so this run stayed in closeout mode, revalidated targeted regressions (`file-list-client`, `move-file-action`, `sftp-ops`, `preferences`, `docker`, `monitoring`, `image-bed`) at 48/48 plus `npm run typecheck`, `npm run lint`, and `./deploy/smoke-test.sh whrkhldsb.qzz.io vcontrolhub` 24/24, confirmed `qa_cron_*` count 0, and updated maintenance state so the next clean-repo priority is the real Quick Services Docker lifecycle canary rather than repeating stale dirty-state cleanup.
- Next QA queue priority starts with deep side-effect paths rather than easy polish: SFTP upload/download/direct access, download task lifecycle, Quick Services Docker/task side effects, backup/restore guardrails, and Settings runtime-effect verification.
- Evidence profiles are now feature-type specific: Files/SFTP requires browser + API + real filesystem/SFTP + DB/index evidence; background-task surfaces require API + task/process + DB + logs + regressions + production checks; Settings requires persisted config plus real runtime/UI effect. Pure 307/401/200 probes remain insufficient.
- Low-value change brake: pure wording, minor spacing/color, static-scan-only candidates, unrelated aria polish, and no-behavior code tidying should be recorded as candidates rather than fixed immediately unless they directly block the selected path or create misleading success/failure/risk states.

## Current blockers

- 2026-06-03 — Quick Services Docker lifecycle blocker resolved by a real authenticated AList 云盘 install/start/stop/uninstall canary: Docker is available and the stale no-Docker proof has been superseded. No active blocker remains for this lane; next focus should rotate to the next backlog canary.


### 2026-06-07T23:05:19Z — AI Provider / QuickService app-source URL boundary closeout
- Scope: lightweight run 41/42 with a focused SSRF/egress trust boundary fix for operator-configured AI Provider base URLs and QuickService custom app-source URLs.
- Fix: added a shared public HTTP(S) URL normalizer that rejects credentials, localhost, loopback, private IPv4/IPv6, link-local/special ranges, and `metadata.google.internal`; AI Provider create/update, app-source creation, and app-source fetch now pass through it while the default OpenAI Base URL remains `https://api.openai.com/v1`.
- Verification: `git diff --check`; targeted Vitest `src/lib/ai/service.test.ts`, `src/lib/quick-service/__tests__/adapters.test.ts`, and `src/lib/storage/__tests__/schema.test.ts` passed 11/11; `npm run typecheck`; `npm run lint -- --max-warnings=0`; full `npm run verify` passed with 208 files / 944 tests plus Next build/runtime/deploy-assets.
- Deployment/status: repaired `.next`/`dist` ownership, restarted `vcontrolhub-next.service` and `vcontrolhub-ssh-ws.service`, reloaded Caddy, retried through an initial restart-window 502 to `/api/status` 200 healthy 3/3, smoke passed 25/25, and post-restart Next/SSH-WS logs were clean of new warning/error/failure matches.
- Cleanup/limits: no temporary QA user, Docker container, SFTP/file, or DB side effect was created; unrelated media diff was reverted. DNS rebinding and redirect-to-private defenses remain future network/egress-policy candidates.


### 2026-06-08T01:51:15Z — Servers overview dirty half-slice closeout
- Scope: preflight found a dirty worktree with only `src/app/servers/server-overview-card.tsx`; dirty-worktree policy selected closeout-only and did not open a new QA lane.
- Finding/fix: the diff was a half-finished diagnostic run state slice (`DiagnosticRunState` plus unused state) with no rendered UI, API call, or production-verifiable side effect, so it was removed to restore a clean product tree.
- Evidence: `git diff --check` passed; targeted Servers/SSH Vitest passed 2 files / 4 tests; `/api/status` returned healthy 3/3; `./deploy/smoke-test.sh whrkhldsb.qzz.io vcontrolhub` passed 25/25; vcontrolhub-next, vcontrolhub-ssh-ws, caddy, and docker stayed active; recent service logs were clean.
- Cleanup/git: no temporary QA users, Docker containers, SFTP/filesystem objects, DB rows, restart, deploy, or product commit was required; product worktree returned clean and HEAD matched origin/main `1f92338` before committing this evidence-only closeout.

## Last known healthy baseline

- 2026-05-29 comprehensive audit: git clean, `npm run typecheck` passed, `npm run lint` passed, `npm test` passed (160 files / 612 tests), `npm run build` passed, `npm run build:runtime` passed, production services active, `./deploy/smoke-test.sh whrkhldsb.qzz.io vcontrolhub` passed 19/19.


## Cron cadence update — 2026-05-31T23:03:19Z

- User feedback: the automatic remediation background task felt too frequent at the previous cadence.
- Active full-product QA remediation job: `9e36e64a75ae`.
- New schedule: `every 30m`.
- Policy: keep this half-hour cadence unless the user explicitly asks for a different interval. The longer interval should reduce interruption/noise and give each coherent major round more room for real QA, verification, deployment, and bundled GitHub commits.
- Verification requirement after cadence edits: confirm the cron job still has its full name, prompt, skills, toolsets, `deliver: origin`, and `workdir: /opt/VControlHub` intact; do not leave an ID-only job after schedule updates.


## Dirty worktree closeout — Global Search localization — 2026-06-01T00:06:09Z

- Watchdog alert cause: full-product QA remediation job `9e36e64a75ae` completed a Global Search localization fix and production verification but hit the tool limit before final cleanup/state/commit, leaving the worktree dirty.
- Product fix: Global Search now localizes dialog title, combobox label, placeholder, result labels/categories, empty state, and shortcut footer using the active locale. English mode no longer shows Chinese search labels such as `仪表盘`.
- Regression: `src/components/__tests__/global-search.test.tsx` now renders through `I18nProvider` and covers English locale search UI plus `/quick-services` navigation.
- Cleanup: removed all temporary `qa_cron_*` users; remaining count verified as 0.
- Verification rerun during closeout:
  - `npx vitest run src/components/__tests__/global-search.test.tsx` — 7/7 passed.
  - `npm run typecheck` — passed.
  - `npm run lint` — passed with existing warnings only.
  - Production services `vcontrolhub-next.service`, `vcontrolhub-ssh-ws.service`, and `caddy.service` — active.
  - `https://whrkhldsb.qzz.io/api/status` — healthy 3/3.
  - `./deploy/smoke-test.sh whrkhldsb.qzz.io vcontrolhub` — 19/19 passed.
- Closeout rule: commit/push these verified functional changes and state updates together, then run the lightweight watchdog directly to confirm it is silent.
- Final closeout: committed and pushed as `7beb73c` (`fix: localize global search overlay`); `main` matches `origin/main` at that commit. The lightweight watchdog was rerun with `python3 /root/.hermes/scripts/vcontrolhub_watchdog.py` and exited silently. State file was corrected to the current `every 30m` cadence and the completed/pushed commit hash.

### 2026-06-07T13:57:36.717823+00:00 — SMTP email alert channel closed
- Status: implemented and locally verified.
- Closed README P1 dead path where email/SMTP was selectable but skipped.
- Added SMTP alert recipients setting, settings validation/UI copy, nodemailer delivery helper, alert test-send email delivery, and real alert evaluation best-effort email delivery.
- Evidence: targeted Vitest 5 files / 33 tests passed; typecheck passed; lint passed; full `npm run verify` passed with 204 files / 922 tests and build/runtime/deploy assets.

#### Deploy evidence — 2026-06-07T13:58:24.877779+00:00
- Runtime build passed and production services restarted: vcontrolhub-next active, vcontrolhub-ssh-ws active, caddy active.
- `/api/status` returned overall healthy.
- Smoke test passed: 25/25 checks.
- Post-restart logs: no error/fail/warn/exception/traceback matches.

## 2026-06-07T14:51:58.623692+00:00 Quick Services 卸载闭环与功能完整性巡查
- 修复：快捷服务卸载弹窗新增“同时删除数据目录”选项；默认只删容器/DB 记录并保留数据。
- 安全边界：可选清理仅处理模板记录的 `/opt/`、`/srv/` 下挂载目录，不清理 Docker socket、时区文件或根目录。
- 其他巡查：记录 SFTP 音视频预览缺 Range、公开目录分享缺整目录归档下载、Office/压缩包预览为安全降级入口。
- 证据：targeted quick-service/status tests 33/33 passed；`npm run verify` passed（204 files / 927 tests, Next build, runtime build, deploy-assets）；production restart/reload completed；`/api/status` 200 healthy；`/status` 200 and rendered `未做实时 SSH/网络探测` / `未做实时 SFTP/直连探测`；smoke 25/25 passed；post-restart logs clean。

## 2026-06-07T14:57:45.043531+00:00 Quick Services 卸载闭环部署证据
- 验证：targeted tests 38/38、typecheck、lint、full verify 均通过。
- 部署：build:runtime 后重启 next/ssh-ws，服务 active，`/api/status` healthy，smoke 25/25 passed。
- 生产日志：重启后 readiness 日志检查未发现 error/fail/warn/exception/traceback。

### 2026-06-07 — Media/image-bed navigation closure and production recovery
- Restored production after `.next/BUILD_ID` was missing by rebuilding Next/runtime artifacts, fixing ownership and restarting `vcontrolhub-next`/`vcontrolhub-ssh-ws`; confirmed `vcontrolhub-next`, `vcontrolhub-ssh-ws`, `caddy`, and `postgresql` are active and enabled.
- Closed the user-visible media/image-bed gap: standalone `/image-bed` is no longer exposed in the main/sidebar/global-search navigation; it remains only as a compatibility external-link management/source-audit page reachable from `/media?type=image`.
- Verified `/media?type=image` in production has the media heading, image/video/audio switcher, image upload workspace, and the external-link audit entry; `/image-bed` appears only as that image-mode auxiliary link.
- Validation: targeted media/image-bed/global-search tests passed (5 files / 21 tests), typecheck/lint passed, `npm run verify` passed, and production smoke test passed 25/25.

### 2026-06-07T16:59:52Z — Media visual workspace and image link center redesign
- Redesigned `/media` as a visual media workspace with clear image/video/audio type cards, a recommended workflow panel, and a dedicated image publishing workflow.
- Media cards now render real image thumbnails via `/api/media/:id/stream`, video first-frame covers via the same Range-capable stream endpoint, and a unified audio icon cover fallback.
- Redesigned `/image-bed` as `图片外链中心`: primary actions are media image workspace, cloud-path publishing, link copy/source audit/batch management; legacy drag-drop direct upload is folded behind a compatibility entry.
- Evidence: targeted media/image-bed Vitest suite passed 12/12; typecheck passed; lint passed; full `npm run verify` passed (205 files / 932 tests plus Next build/runtime/deploy assets); production services restarted; `/api/status` healthy 3/3; smoke passed 25/25; authenticated production HTML confirmed `/media` workspace and `/image-bed` link center; browser DOM confirmed image `<img>` thumbnails and video stream covers.

### 2026-06-07T19:07:20Z — Downloads negative-boundary and list canary
- Scope: lightweight run 35/36; no product code changed and no real remote download was started.
- Environment capability: Docker available/running, `vcontrolhub-next.service`, `vcontrolhub-ssh-ws.service`, `caddy.service`, and `docker.service` active/enabled; git worktree was clean and `HEAD == origin/main` at `66193d0`.
- User journey/evidence: temporary authenticated QA session reached `/downloads` with HTTP 200; `/api/downloads` returned HTTP 200 with existing terminal download-task history and `globalStat: null`, proving list hydration without starting aria2/SSH work.
- Boundary evidence: state-changing PATCH without CSRF returned 403 `CSRF token 验证失败`; invalid mixed HTTP + magnet batch POST returned 400 `磁力/BT 链接需单独创建任务，请勿与普通链接混在同一批量中`; refresh for a missing task returned 404 `任务不存在`.
- Production/status/logs: `/api/status` returned HTTP 200 with 3 checks; `./deploy/smoke-test.sh whrkhldsb.qzz.io vcontrolhub` passed 25/25; recent Next/SSH-WS journal scan for error/warn/failed/exception/CSRF/download/aria2/EACCES matches was clean.
- Cleanup: temporary `qa_cron_downloads_*` user deleted, `qa_cron_*` remaining count 0, no canary DownloadTask rows were created, and temporary credential/script files were removed from `/tmp`.

### 2026-06-08T00:33:42Z — Files/Media dense-actions dirty closeout
- Scope: run 42/42 was due for a broad review, but entry worktree was dirty, so policy selected closeout-only for the existing Files/Media/installer slice instead of opening a new QA lane.
- Fixes: `/files` keeps detail/preview/download as primary actions and moves share/rename/move/delete into a More menu with unified toast feedback; media scan infers MIME from filename/path for `application/octet-stream` rows and media player adds previous/next/download/source continuity; installer runtime roots now follow non-default `APP_SLUG` when legacy vcontrolhub defaults leak in.
- Evidence: targeted Vitest 3 files / 25 tests passed; `npm run typecheck` passed; `npm run lint -- --max-warnings=0` passed; full `npm run verify` passed (208 files / 944 tests plus Next build/runtime/deploy-assets); `make installer-fakeroot` passed (23 passed / 7 skipped).
- Production: repaired `.next`/`dist` ownership, restarted `vcontrolhub-next.service` and `vcontrolhub-ssh-ws.service`, reloaded Caddy, `/api/status` healthy 3/3, smoke 25/25 passed, recent next/ssh-ws logs clean.
- Cleanup: no temporary QA users, Docker containers, SFTP/filesystem objects, or DB rows were created; no cleanup required.
