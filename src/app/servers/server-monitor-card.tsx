"use client"; import { useEffect, useState, useCallback } from "react";
import { csrfFetch } from "@/lib/auth/csrf-client";
import { getRefreshIntervalLabel } from "@/lib/preferences/refresh-interval";
import { useRefreshInterval } from "@/lib/preferences/use-refresh-interval"; import { useI18n } from "@/lib/i18n/use-locale";  type CpuInfo = { usagePercent: number; cores: number; loadAvg: [number, number, number] };
type MemInfo = { totalMb: number; usedMb: number; availableMb: number; usagePercent: number };
type DiskInfo = { mount: string; totalGb: string; usedGb: string; usagePercent: number };
type NetInfo = { iface: string; rxBytes: number; txBytes: number }; type Metrics = { cpu: CpuInfo; memory: MemInfo; disk: DiskInfo[]; network: NetInfo[]; uptime: string; timestamp: string;
}; type Props = { serverId: string; serverName: string;
};  function formatBytes(bytes: number): string { if (bytes>= 1_073_741_824) return `${(bytes / 1_073_741_824).toFixed(1)} GB`; if (bytes>= 1_048_576) return `${(bytes / 1_048_576).toFixed(0)} MB`; if (bytes>= 1024) return `${(bytes / 1024).toFixed(0)} KB`; return `${bytes} B`;
} function usageColor(pct: number): string { if (pct>= 90) return "bg-[var(--danger)]"; if (pct>= 70) return "bg-[var(--warning)]"; return "bg-[var(--color-action-bg)]";
} function usageTextColor(pct: number): string { if (pct>= 90) return "text-[var(--danger)]"; if (pct>= 70) return "text-[var(--warning)]"; return "text-[var(--color-action)]";
}  function ProgressBar({ value, max = 100, className = "" }: { value: number; max?: number; className?: string }) { const pct = Math.min(100, Math.max(0, (value / max) * 100)); return ( <div className={`h-1.5 rounded-full bg-[var(--surface)]/[0.10] overflow-hidden ${className}`}> <div className={`h-full rounded-full transition-[width] duration-700 ${usageColor(pct)}`} style={{ width: `${pct}%` }} /> </div> );
} function MetricRow({ label, value, unit, pct }: { label: string; value: string; unit?: string; pct?: number }) { return ( <div className="flex items-center justify-between gap-2 text-xs"> <span className="text-[var(--text-muted)] shrink-0">{label}</span> {pct !== undefined && <ProgressBar value={pct} className="flex-1 min-w-[40px]" />} <span className={`font-mono tabular-nums ${pct !== undefined ? usageTextColor(pct) : "text-[var(--text-primary)]"}`}> {value}{unit && <span className="text-[var(--text-muted)] ml-0.5">{unit}</span>} </span> </div> );
}  export function ServerMonitorCard({ serverId }: Props) { const { t } = useI18n(); const [metrics, setMetrics] = useState<Metrics | null>(null); const [error, setError] = useState<string | null>(null); const [loading, setLoading] = useState(true); const refreshIntervalSeconds = useRefreshInterval(30); const fetchMetrics = useCallback(async () => { try { const data = await csrfFetch(`/api/servers/monitor?serverId=${encodeURIComponent(serverId)}`) as Metrics & { error?: string }; if (data.error) { setError(data.error); setMetrics(null); } else { setMetrics(data); setError(null); } } catch (err) { setError(err instanceof Error ? err.message : t("serverMonitor.error.fetch")); setMetrics(null); } finally { setLoading(false); } }, [serverId]); useEffect(() => { const timer = window.setTimeout(() => { void fetchMetrics(); }, 0); if (refreshIntervalSeconds <= 0) return () => window.clearTimeout(timer); const interval = setInterval(() => { void fetchMetrics(); }, refreshIntervalSeconds * 1000); return () => { window.clearTimeout(timer); clearInterval(interval); }; }, [fetchMetrics, refreshIntervalSeconds]); if (loading) { return ( <div data-card className=" p-4 space-y-2 animate-pulse"> <div className="h-3 w-24 bg-[var(--surface)]/[0.10] rounded-lg" /> <div className="h-3 w-full bg-[var(--surface)]/[0.10] rounded-lg" /> <div className="h-3 w-full bg-[var(--surface)]/[0.10] rounded-lg" /> <div className="h-3 w-3/4 bg-[var(--surface)]/[0.10] rounded-lg" /> </div> ); } if (error) { return ( <div data-tone="rose" className="rounded-xl border border-[var(--danger-border)] p-4"> <div className="flex items-center gap-2 text-xs text-[var(--danger)]"> <span>⚠</span> <span>{t("serverMonitor.error.connection")}{t("common.colon")}{error}</span> </div> <button onClick={fetchMetrics} className="mt-2 text-xs text-[var(--danger)] hover:text-[var(--danger)] transition"> {t("common.retry")} </button> </div> ); } if (!metrics) return null; const { cpu, memory, disk, network, uptime } = metrics; return ( <div data-card className=" p-4 space-y-3"> {} <div className="flex items-center justify-between"> <h4 className="text-xs font-medium text-[var(--text-primary)]/70 uppercase tracking-wider">{t("serverMonitor.title")}</h4> <div className="flex items-center gap-1.5"> <div className="h-1.5 w-1.5 rounded-full bg-[var(--success)] shadow-[0_0_4px_rgba(52,211,153,0.5)] animate-pulse" /> <span className="text-[10px] text-[var(--text-muted)]">{new Date(metrics.timestamp).toLocaleTimeString("zh-CN")}</span> <span className="text-[10px] text-[var(--text-secondary)]">· {getRefreshIntervalLabel(refreshIntervalSeconds)}</span> </div> </div> {} <div className="space-y-1.5"> <div className="flex items-center justify-between text-[11px] text-[var(--text-muted)]"> <span>{t("serverMonitor.cpu")}</span> <span className="text-[var(--text-muted)]">{cpu.cores} {t("serverMonitor.cores")}</span> </div> <ProgressBar value={cpu.usagePercent} /> <div className="flex items-center justify-between"> <span className={`text-sm font-semibold tabular-nums ${usageTextColor(cpu.usagePercent)}`}> {cpu.usagePercent.toFixed(1)}% </span> <span className="text-[10px] text-[var(--text-muted)] font-mono"> {t("serverMonitor.load")} {cpu.loadAvg[0].toFixed(2)} / {cpu.loadAvg[1].toFixed(2)} / {cpu.loadAvg[2].toFixed(2)} </span> </div> </div> {} <div className="space-y-1.5"> <div className="flex items-center justify-between text-[11px] text-[var(--text-muted)]"> <span>{t("serverMonitor.memory")}</span> <span className="text-[var(--text-muted)]">{memory.usedMb} / {memory.totalMb} MB</span> </div> <ProgressBar value={memory.usagePercent} /> <span className={`text-sm font-semibold tabular-nums ${usageTextColor(memory.usagePercent)}`}> {memory.usagePercent.toFixed(1)}% </span> </div> {} {disk.length> 0 && ( <div className="space-y-1.5"> <span className="text-[11px] text-[var(--text-muted)]">{t("serverMonitor.disk")}</span> {disk.map((d) => ( <MetricRow key={d.mount} label={d.mount} value={`${d.usedGb} / ${d.totalGb}`} pct={d.usagePercent} /> ))} </div> )} {} {network.length> 0 && ( <div className="space-y-1.5"> <span className="text-[11px] text-[var(--text-muted)]">{t("serverMonitor.network")}</span> {network.map((n) => ( <div key={n.iface} className="flex items-center justify-between text-xs text-[var(--text-secondary)]"> <span className="text-[var(--text-muted)]">{n.iface}</span> <span className="font-mono text-[11px]"> ↓{formatBytes(n.rxBytes)} ↑{formatBytes(n.txBytes)} </span> </div> ))} </div> )} {} {uptime && ( <div className="pt-1 border-t border-[var(--border)] text-[11px] text-[var(--text-muted)]"> {t("serverMonitor.uptime")} {uptime} </div> )} </div> );
}
